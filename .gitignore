__pycache__/
*.pyc
.venv/
.env
.idea/
.DS_Store
*.egg-info/
.pytest_cache/
dist/
build/

# Local Claude / IDE config — not part of the project
statusline-command.sh
.claude/
node_modules/

# Runtime data + local artifacts (not source)
var/
*.log
.playwright-cli/
ruvector.db
prompts.zip

# Ruflo / claude-flow local runtime state — machine-local, not committed
.swarm/
.claude-flow/

# Project worklog — local only, not committed
docs/memory/
