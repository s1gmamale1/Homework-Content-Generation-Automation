__pycache__/
*.pyc
.venv/
.env
.idea/
.DS_Store
*.egg-info/
.pytest_cache/
dist/
build/

# Local Claude / IDE config — not part of the project
statusline-command.sh
.claude/
node_modules/

# Ruflo / claude-flow local runtime state — machine-local, not committed
.swarm/
.claude-flow/

# Runtime data + local artifacts (not source)
var/                  # uploaded PDFs + generated screenshots
*.log                 # uvicorn / retry logs
.playwright-cli/      # playwright skill cache
ruvector.db           # ruflo MCP local store
prompts.zip           # local backup archive
