__pycache__/
*.pyc
.venv/
.env
.idea/
.DS_Store
*.egg-info/
.pytest_cache/
dist/
build/

# Local Claude / IDE config — not part of the project
statusline-command.sh
.claude/
node_modules/

# Runtime data + local artifacts (not source).
# NOTE: .gitignore does NOT support inline (trailing) comments — a comment
# after a pattern on the same line makes the whole line a literal path and the
# pattern silently stops matching. Keep comments on their own lines.
# uploaded PDFs + generated screenshots
var/
# uvicorn / retry logs
*.log
# playwright skill cache
.playwright-cli/
# ruflo MCP local store
ruvector.db
prompts.zip           # local backup archive
Backend-terms.md
# Brainstorming visual companion (local mockups)
.superpowers/
.mcp.json
.cursor
.swarm
# Local Google service-account credential — never commit
phillhr57@gmail.com.json
.codex
AGENTS.md
# PC-transfer kit (contains DB dumps) — never commit
pc-transfer.zip
.claude-flow
classarecruiter20@gmail.com.json
abdscorpion0@gmail.com.json
# Catch any future email-named service-account key dropped in the tree
*@*.json

# Manual test scratch — sample custom prompts uploaded by hand, not part of the app
samples/

# Local QA harness — student-simulation teaching test (not part of the app)
tools/teaching_test/

# Local operations runbooks — host-specific ops notes, not part of the app
/docs/operations/
