# Project Memory — Index

> Titles only. One line per task, newest at the bottom.
> Full detail for each entry lives in [MASTER_MEMORY.md](./MASTER_MEMORY.md) under the matching `## [ID]` heading.

| ID | Date | Branch | Title |
|----|------|--------|-------|
| 0001 | 2026-05-29 | Nggaev | Set up project memory system (INDEX + MASTER_MEMORY) |
| 0002 | 2026-05-29 | Nggaev | Document memory system in CLAUDE.md for other sessions |
| 0003 | 2026-05-29 | Nggaev-v2 | Cut Nggaev-v2 off DaddysBranch (CLI router) + ported flow-v2 docs/registry |
| 0004 | 2026-05-29 | Nggaev-v2 | Phase 1: Flow v2 schemas + provider fallback + task→model policy (TDD, 67 green) |
| 0005 | 2026-05-29 | Nggaev-v2 | Adopt rewritten (codebase-grounded) plan; prune Phase 1 to PR-scope (55 green) |
| 0006 | 2026-05-29 | Nggaev-v2 | PR-1: source map — extract_source_map + persist source_map_json (TDD, 58 green) |
| 0007 | 2026-05-29 | Nggaev-v2 | PR-4: Boss Arena (Why→How→What) content phase, replaces final-challenge in flows (TDD, 64 green) |
| 0008 | 2026-05-29 | flow-v2-integration | Integrate teammate PRs: Learning(PR#3) DONE+verified (75 green); Practice/alphaq(PR#2) NOT usable as-is; reflection KEPT per New_Flow.md; consolidation→remove; Practice Arc pending |
| 0009 | 2026-05-29 | flow-v2-integration | PR-3 Practice Arc: 6 typed games (3 schemas — RLC + ErrorDetection + shared CbpModeGame), curated per-subject arcs, removed game-breaks/real-life/consolidation, kept reflection; 168 green + claude smoke OK; plan §6/§9 patched |
| 0010 | 2026-05-29 | Nggaev-v2 | PR #1 (molotov) assessed: parallel Phase 2, conflicts — NOT merged. Harvested plan-endorsed only: SourceMap threading (§10 fidelity, smoke OK) + empty-retry + subset-PDF extract; added opencode provider (W1 done). Skipped registry/QA/division-mapper. 190 green |
| 0011 | 2026-05-29 | Nggaev-v2 | PR-5 assembly reshape (plan §8: title/book/summary/Source Map/divisions/reflection; pure renderer; double-heading fix); migration chain 0012-0015 verified offline (single head, clean DDL — live apply needs Docker). All 5 Flow v2 PRs done. 197 green |
| 0012 | 2026-05-30 | Nggaev-v2 | Live real-textbook smoke + fixes: gemini_api_key optional, .gitignore inline-comment bug (almost committed copyrighted PDF), opencode stdin (positional broke on Win 32k limit), gemini -p "." (newer cli rejects empty). FINDINGS: claude refuses copyrighted extraction; opencode/deepseek too weak (0 TOC); gemini is the extractor; TOC reads only first ~10 pages (misses back-of-book contents). DB live on :5433 |
| 0013 | 2026-05-30 | Nggaev-v2 | Flashcards 32k-SVG failure (claude CLI ceiling, per-card inline SVG) + fix (flashcards out of _SVG_PHASES; bracket notation in geometry/biology/english prompts); deep per-phase plan verification (6 audit subagents — largely on-track); teacher-note labeling §8 (🔑 TEACHER NOTE marker across 8 synth branches); CBP slot-7 prose reconciled (kept collapsed model); HOW_IT_WORKS.md added; final packet (41,717 chars, 40 teacher notes) exported to Downloads. 203 green. Committed 263d392 |
| 0014 | 2026-05-30 | Nggaev-v2 | TOC extraction fix: real root cause was a glyph-subset font (pypdf emits /Gxx garbage), not page coverage → glyph-decode + front/tail + reverse-tail scan + gemini keeps PDF attached. Live: 0→81 TOC entries on the real book. Plus plan-conformance hardening after integrity-checking a (stale) external audit: #2 concept-id source-fidelity validation (detect+surface, non-fatal), #5 RLC reverse-test variant (4 subjects), docstring nit. Audit #3/#6 were wrong (plan already correct), #4 already done, #1 deliberate. 214 green |
| 0015 | 2026-05-30 | Nggaev-v2 | CBP Completeness (Flow v2 reshape WS1): added the 2 mandated Learning Blocks + required why_wrong_fails; fidelity collector covers LB concept ids; synth interleaves C1→LB1→C2→LB2→C3; 7 CBP + 11 CBP-mode game prompts updated. Spec+plan in docs/superpowers. Subagent-driven, each task diff+suite verified. 219 green. Live smoke OK (blocks populated, no 32k crash) — WATCH: CBP ~46k out-tokens / ~12min, heaviest phase. Reverses 0013 "keep collapsed" (standard requires blocks). 4 WS remain |
| 0016 | 2026-05-30 | Nggaev-v2 | Flashcards Completeness (Flow v2 reshape WS2): 3-field card → spec 8-field active-recall card. type+difficulty required closed enums; explanation/example/misconception optional; front/back min_length. Synth renders type·difficulty + optionals. 7 subject prompts rewritten. Subagent-driven; controller verification caught + fixed contradictory legacy rules a subagent left (commit 9a1a061). 224 green. Live smoke OK (12 cards, all typed, no svg, 13.8k tok/3.5min). Deck-only (study games out of scope). 3 WS remain |
| 0017 | 2026-05-30 | Nggaev-v2 | Memory Check Completeness (Flow v2 reshape WS3): flat options+correct_index → MemoryCheckOption{text,is_correct,reason} + MemoryCheckBlank{answer,accepted_variations} + enriched item; kind-keyed validator (MCQ/CCE: exactly 4 opts+1 correct; fill_blank: blanks-only). Synth keeps §8 teacher-note split. 7 prompts rewritten (subagent self-cleaned old format). 228 green. Live smoke OK (10 items all kind-valid, distractor reasons, 12.3k tok/3min). 2 WS remain (practice-game payloads, language contract) |
| 0018 | 2026-05-30 | Nggaev-v2 | Practice-Game Payloads (WS4): typed interaction_payload per CBP-mode game (pairs/pieces+types/sentence+chips/9-cell grid) + mode-match validator + synth + 11 prompts. 233 green. ⚠ BLOCKER: live jigsaw gen ran >21min on ONE call (killed) — CBP-mode games inherit the FULL CBP shell + 2 learning blocks + payload → too heavy. Code correct & green, but games not usable until a LIGHTER game-skeleton redesign (new required follow-up). Remaining: WS5 language contract + game-skeleton lightening |
| 0019 | 2026-06-01 | Nggaev-v2 | General Prompts MVP (Path A): `prompts/_general/` (7 files) serve ALL subjects via `get_prompt` resolver (`{{SUBJECT}}` from SUBJECT_LABELS; `USE_SUBJECT_PROMPTS=False` future override switch); single `GENERAL_FLOW`/`flow_for` (no easy/hard); classify removed (schema map + pipeline, difficulty pinned None). Per-subject dirs + flow.md/instruction.md/classify.md DEAD but LEFT UNTOUCHED (override layer when switch flipped). 222 green; claude boss-arena smoke PASS (physics, 5 Q, why/how/what+concept_ids≥1). Commits daa9008·24a2b67·fe1bc6d·8a4a8a1·5a3660b·b1d758c·6738336. Next: Path B — lighten CbpModeGame off full CBP ([[0018]]) + SUBJECT_GAME map + 4 CBP-mode general prompts |
| 0020 | 2026-06-01 | Nggaev-v2 | English Target-Language Handling: `get_prompt` substitutes `{{LANGUAGE_RULES}}` per subject — `english`→English target content + Uzbek "Siz" scaffolding (CEFR-leveled), else formal Uzbek. Fixes live defect (english lessons were Uzbek prose ABOUT English). NEW grade→CEFR ladder G5→A1…G11→B2 (assumption; classify-based CEFR gone). 7 `_general` prompts use the token; flashcards English-conditional front/back hint. 226 green; claude smoke PASS on flashcards+memory-check (split correct: EN target front / UZ bridge back+reasons; CEFR ~A2). Commits 0226233·f54fd8a·d893538·909a73d. Caveat: inert prompts/english/* still ref deleted classify.md (fix when USE_SUBJECT_PROMPTS revives) |
| 0021 | 2026-06-01 | Nggaev-v2 | CBP-Mode Game Lightening (Path B): `CbpModeGame(CaseBasedPreview)`→compact `CbpModeGame(BaseModel)` (title/source_concept_ids/interaction_mode/instruction/typed payload/light `why_prompt`, NOT full DPE) — clears the [[0018]] >21min blocker. `SUBJECT_GAME` map + 8-phase `flow_for` (one subject-matched game re-added; `GENERAL_FLOW` removed); 4 compact `_general` game prompts ({{SUBJECT}}+{{LANGUAGE_RULES}}); compact synth render with §8 fix (correct cells/chips teacher-marked, not inline ✓ — plan corrected). 227 green; real claude smoke physics→practice-tictactoe schema-valid in **71.2s** (vs 21min), 9 cells/3 correct, 3 real source concept_ids, Uzbek. Commits a6b9ece·9343559·f3a9ad9·00f97a0. Deliberate deviation from full-CBP Gamified-Practices specs (measured). Next: WS5 Uzbek language contract + cheap-model re-benchmark |
| 0022 | 2026-06-02 | Nggaev-v2 | Backlog R2–R8 backend fix pass (brainstorm→spec→plan→subagent-driven, controller-verified each commit). R2 size-gate extract subset (no wrong-page slice <20MB); R3 reflection conform (drop score-branch/consolidation/easy-hard/typo, add debrief structure); R4 4 Literal mode subclasses pin game phase→mode (+ __init__ export gotcha); R5 fail-fast validators (checkpoint/simulation/tictactoe/memory-match/jigsaw-`solution`/error-detection); R6 retain TOC task ref + hoist >20MB guard + `_STATS_PROVIDERS` from registry; R7 job_timeout 600→1800; R8 trim `_SVG_PHASES` (drop boss/rlc/error/4-games, keep CBP). 227→240 green. Commits 40dab51·576a529·aedea9d·3e5053e·f40366b·bebf730·e3988a1. FLAG: frontend adds optional `solution` to jigsaw TS type (forward-only) |
| 0023 | 2026-06-02 | Nggaev-v2 | Notion Integration Phase 1 (PUSH): subagent-driven build (spec→plan→11 tasks, 2-stage review each). New `app/services/notion/` (pure `blocks`, sync rate-limited `client` w/ 2-step file upload, idempotent `page_creator`) + best-effort `notion_archive.archive_job` (OFF by default) + shared `job_artifacts` + migration 0016/`c9e3f1a07b62` (`books.grade`, `homework_jobs.notion_archived_at`, `toc_entries.notion_homework_page_id`) + pipeline hook after job-done + config `notion_enabled`/`notion_api_key`/`notion_subject_pages`. 245→265 green. Commits e5a4c93·6303588·0bb9e66·cc9a727·9567a6b·4244cb2·755d6f7·8e16852·3d97025·16a6564·941ec62·7dd8823 (+spec/plan cfa9800), PUSHED. **LIVE SMOKE PASSED** on real Notion via user's `ntn_` token: ran archive on existing done job `442cc7c4` (Geometriya-8 §1.1) → created lesson + Homework page under the Geometriya subject page, full content rendered + `homework.md`+`content.json` attached, DB stamps written (page `37399838-1c76-81e8-a547-db74b8c1dbbc`). All deferred concerns empirically cleared (write capability, `properties.title` works WITHOUT `"type":"text"`, 2-step upload, 100-block chunking). Logged **R9** (inline SVG/tables render as escaped text in blocks — accepted limit; attachments lossless) + **R10** (broken-font Geometriya 2019 PDF → glyph-garbage poisons TOC local-text → 1 section vs English 22 / Algebra full; NOT size/scan/20MB). Re-verified WISHLIST non-stale + sorted Backend/Frontend. Deferred: frontend grade input, Notion Phase 2 (PULL) |
| 0024 | 2026-06-02 | Nggaev-v2 | Frontend fixes — the 3 WISHLIST Frontend items (controller-direct, not subagent; small). `job.tsx` `DonePanel` now counts Flow v2 columns (source concepts·case checkpoints·memory checks·Boss Arena w/ legacy final-challenge fallback·practice-arc games) so v2 jobs show real done-stats; `upload.tsx` optional **Grade** select (1–11) wired `api.uploadBook`→`POST /books` `grade` (no SQL for NEW uploads; existing NULL-grade books still need SQL/re-upload); `section.tsx` model Select `value={model ?? ""}` drops the uncontrolled→controlled warning. Verified: `tsc -p tsconfig.app.json --noEmit` exit 0 + `npm run build` OK (dist rebuilt). Biome exit 1 = **pre-existing** repo noise only (CRLF/import-order/a11y-label/exhaustive-deps), none from these edits. Commit `e582f53` (+ docs `b57cc02`). NOT browser/live-verified (user testing). Corrected a standing error: `docs/memory/` is **tracked**, not gitignored. WISHLIST Frontend section → empty; trio moved to Done |
| 0025 | 2026-06-02 | Nggaev-v2 | Content-gen **default provider → claude / claude-sonnet-4-6** (was gemini). `agent_models.py`: claude manifest lists `claude-sonnet-4-6` first (was `claude-opus-4-7`) — fixes a latent inconsistency (manifest "recommended" was opus while `_PROVIDER_DEFAULT_MODEL["claude"]` is sonnet; now both sonnet). `section.tsx:33` picker default `gemini→claude` (auto-selects `[0]`=sonnet via the manifest effect). `api.ts` `generate()` fallback `gemini→claude`. **Extractor pin intentionally UNTOUCHED** (`config.py` `settings.extract_provider/extract_model` = gemini / gemini-2.5-flash — user explicitly flagged). Verified: 26 backend tests pass (resolver invariants incl. gemini-no-default-leak intact), `tsc` 0, vite build OK. Commit `18ff032`. Context: a Kimyo job was accidentally run on `gemini-3.1-pro-preview`; user wanted claude to be the default going forward |
| 0026 | 2026-06-02 | Nggaev-v2 | **Fix: section status badges intermittently vanishing** (diagnosed via systematic-debugging code-trace). `book.tsx` sets `entries` from BOTH REST `getBook` (enriched w/ `latest_job_status` via `jobs_repo.latest_by_section`) AND the SSE `/toc/stream` `toc_ready` replay (`books.py:126-129`) which built entries with **no enrichment** → race: the lighter/faster SSE query fires immediately and overwrote the enriched entries → badges gone (esp. after hard refresh). Root cause = **duplicated + drifted** enrichment between the two endpoints. Fix: extracted one shared `_enriched_toc_entries(session, book)` helper used by BOTH REST `_book_out_with_toc` AND the SSE replay → consistent shape, race harmless. TDD `tests/api/test_toc_status_enrichment.py` (red→green). Backend-only → **takes effect on server restart**. Suite 265 passed + 1 PRE-EXISTING unrelated red (`test_config_notion::test_notion_defaults_disabled` — `.env` has `NOTION_ENABLED=true`, test doesn't isolate; memory NEXT #1). Commit `4c9fe63`. Bonus: Kimyo **§3 Notion push live-verified** (job `f8b1214c` done, `notion_archived_at` stamped) — 2nd subject end-to-end |
| 0027 | 2026-06-03 | Nggaev-v2 | Notion subject-page map: full grade-8 (all 6 mapped app subjects) + **history split-by-filename routing**. `notion_subject_pages` value widened `str` → `str \| {keyword: page-id}`; `_resolve_subject_page_id` gains a `hint` (book filename) arg — string value passes through (covers normal subjects + combined-history grades), dict value folds the filename and substring-matches a keyword (jahon→Jahon tarixi, ozbekiston→O‘zbekiston tarixi), no match → None (clean skip, never mis-files). One app `history` subject + one prompt unchanged. Controller-direct TDD (3 new resolver tests red→green; 7 notion_archive pass). End-to-end verified against real `.env` (union parses from env JSON; both history books route correctly; physics string + unknown→None). `.env` map filled for grade 8 (gitignored). Pre-existing `test_notion_defaults_disabled` still red (backlog NEXT #1, unrelated). Files: `notion_archive.py`·`config.py`·`tests/services/test_notion_archive.py`. NOT committed (user tests/commits) |
| 0028 | 2026-06-03 | Nggaev-v2 | **Effort A — md-per-phase architecture flip (SHIPPED + live-verified).** Generation reshaped: every phase now emits ONE markdown file (no JSON/schema/assembly/`content.json`); `phase_outputs.output_md` is the deliverable. New deterministic warn-only `phase_validator` records `validation_warnings` per phase (surfaced in console). Notion archive → ONE sub-page per phase under Homework (rendered md + attached `.md`); download → zip of `<NN>-<phase>.md`; preview renders per-phase md; 10 `_general` prompts converted JSON→md + visual convention (inline `<svg>` / `![placeholder…](placeholder)`). Removed: `STRUCTURED_PHASE_SCHEMAS`, `run_phase_prompt_structured`, `_synth_md_for_structured`, `_assemble`/`_render_homework_md`, `_JSON_COLUMN_SETTERS`, all `set_*_json`, `structured_artifacts`/`build_content_json`, `extract_source_map`/source-map, 9 schema modules, 16 `homework_jobs` cols (migrations 0017 add-warnings · 0018 drop-cols). Spec+plan in docs/superpowers; 11 tasks subagent-driven, controller-stress-tested each commit (read diff + re-run). Suite 280→179 (deleted structured-layer tests; **protected** `_resolve_model` leak-guard + `extract_toc` kept) +1 pre-existing red; tsc 0 + build OK. **LIVE SMOKE PASS**: Kimyo §1 on claude/sonnet-4-6, 9/9 phases all markdown (real Uzbek chem, H₂SO₄ Mr=98→N=0.5mol), validator flagged reflection's missing `#`, **8 Notion sub-pages** created + `.md` attached (Homework `37499838-1c76-81be-98ab-f64cccc82110`), download zip = 8 `.md`. Commits 15e9e19·7333bf0·6aadefe·31cdbcf·abf6b7d·934a9fd·8731ccf·52dfa58·dc4a99a·1ac49de·7ba2143. Backup `backup/md-per-phase-pre-teardown`. NEXT = **Effort B** (faithful Infra-spec prompt rewrite + per-phase validator rules; give reflection a `#` title) |
| 0029 | 2026-06-03 | Nggaev-v2 | **Notion homework page regroup + attachments-at-top.** Replaced the 8 flat phase sub-pages with a grouped tree under `Homework` (`_HOMEWORK_LAYOUT` in `notion_archive.py`): **Case-Based Preview** · **Flashcards** (flashcards + memory-check rendered inline on ONE page) · **Gamified Practices** (a *container* page with Real-Life Challenge / Error Detection / `<subject game>` as child sub-pages) · **Boss Arena** · **Reflection**. Boss Arena stays its own page (excluded from games). Every content page now puts its `.md` attachment(s) at the **very top** (above the rendered md), mirroring the textbook PDF on a lesson page; one attachment per phase kept. `_push_to_notion` now takes a `phase_md` dict (was a `(title,phase,md)` list); `_leaf_blocks` helper renders attachments-first then content sections; idempotent per page (skip if `page_has_content`). TDD: 3 new structure tests (full-tree call sequence, flashcards 2-attachments-at-top, skip-populated) — 8 notion_archive tests pass; suite 180 + 1 pre-existing red. **Live-verified** against real Notion via a throwaway lesson under the Kimyo subject page (read the tree back through `NotionClientWrapper`: 5 top-level pages, Flashcards `['file','file',...]`, Gamified Practices container w/ 3 game children) then deleted the throwaway. Commit pushed. NOTE: only NEW archives use this layout; previously-archived lessons keep the old flat structure (no Notion migration in scope). |
| 0030 | 2026-06-04 | Nggaev-v2 | **Effort B — faithful Infra-spec prompt rewrite (family-aware, markdown) — SHIPPED + live-verified.** Rewrote `prompts/_general/*.md` to the `docs/Infra_prompts` specs; purged Effort-A dead JSON-field vocab. Mechanism: Option-A dict-backed `{{FAMILY_RULES}}` in `prompts.py` — `_SUBJECT_FAMILY` (7 subjects→4 families) + `FAMILY_RULES[phase][family]` named blocks, resolved like `{{LANGUAGE_RULES}}` with a no-cross-family-leak guard + token-no-entry collapse; only CBP+Flashcards carry blocks. Tier 1: CBP+Flashcards family-aware (4 blocks each, **humanities CBP authored** — no source spec; atomisation examples ported verbatim). Tier 2: memory-check (≥2-of-3 + distractor rule), rlc+error-detection (Strip Test), boss-arena (weak-skill adaptation + Why/How/What), 4 games (compact divergence, `"checkpoint 3" not in` guard), reflection (`#` title fix). Reviewer notes folded: flashcards enum in-prompt only (schema deleted), placeholder-sentinel form, token-no-entry. Bonus: fixed pre-existing `tmp_*` fixture cache-pollution (teardown). spec→plan→subagent (10 tasks), controller stress-tested each commit; CBP authoring got independent spec-faithfulness review (clean). Suite **195 + 1 pre-existing red**. **LIVE PASS** real gemini history/humanities (job `defe06b6`): 8 phases all md, validator-clean/0 dead-vocab/0 json-fence, CBP full 10-section + structural SVG, flashcards causal-discipline misconception. Commits e118f03·f0b80a8·5be1c6f·ae049db·99cb49b·5383d94·b0bf697·9b49c67·9b6dd9f; spec `0dbb34a`, plan `61dc6c6`. NEXT = per-phase validator RULES + WS5 Uzbek; Notion lesson-page-matching spec `299f5b1` build-ready |
| 0031 | 2026-06-04 | Nggaev-v2 | **Job Resilience — phase resume + provider failover + lease-TTL reclaim (SHIPPED + live-verified).** Jobs survive interruption: 4 mechanisms over a "rebuild phase context from persisted rows" foundation. T1 config (heartbeat/lease/per-attempt-timeout/failover-order, **claude excluded from fallback**); T2 additive `phase_outputs.provider` (mig 0019 `a7c1e9d2b4f8`); T3 claim **heartbeat** (`touch_claim` refresh while running); T4 **lease-TTL reclaim** (`reclaim_stale_seconds`, was `job_timeout×2`) + startup orphan-reset (reviewer caught a silent-no-op commit-placement bug); T5 pure **classifier** (transient/wall/hard, transient-before-wall); T6 **failover** (`_run_with_failover` injectable, `asyncio.TimeoutError`→immediate failover, records producer) + `_execute_phase` wiring; T7 **resume** (skip+reuse done phases, always-on; `force`=new job, `/retry`=resume — ratified). 8 tasks subagent-driven, controller stress-tested each commit; **two** fresh-session reviews folded (incl. `per_attempt_timeout` 300→600 vs CBP ~274s, `_sweep_stuck_jobs` name, lifespan commit placement). Suite **218 + 1 pre-existing red**; mig round-trip verified. **LIVE SMOKE PASS** (opencode job `6a760767`, claude budget untouched): resume skipped 5 done phases, failover opencode→codex→gemini on all 8 content phases (attribution `provider=gemini`), opencode hung 600s/wave → job_timeout→retry→resume recovered in 2 attempts → `done`; the ratified timeout-budget interaction confirmed live; controller mis-diagnosis (heartbeat-bug alarm) owned & corrected by the server log. Commits `6beb25d`·`44af511`·`f124377`·`17ca630`·`b805ee4`·`515d056`·`4925edf`·`213574f`. Findings: **R11** (failover not legibly recorded — model_name stale, timed-out attempt unlogged) + WISHLIST (opencode flaky, last-resort only). NEXT = R11 observability; provider-aware per-attempt timeout; multi-pod-safe startup reset |
| 0032 | 2026-06-04 | Nggaev-v2 | **Notion lesson-page matching — word-match adoption + Generated Lessons fallback.** Archive now places `Homework` fully automatically (no operator — autonomy): app lesson title **uniquely** word-matches a human Notion lesson page → write Homework INSIDE it (co-located with Adrian's Text/Video); else → app-owned `Generated Lessons` container under the subject. Fixes prior accidental exact-title match (kimyo co-located, history/algebra parallel). `_HOMEWORK_LAYOUT` subtree (worklog 0029) untouched — only the lesson page's parent changed. New pure `app/services/notion/lesson_match.py`: `tokenize` (drops numbers/§/punct/**page-leaders incl. U+2026 ellipsis + ASCII dots**/markers; folds apostrophes NOT diacritics) + `match_lesson` (app words ⊆ human words, **unique**, ≥2-word guard, container excluded). Wired at `_push_to_notion:133`. **subset+unique** strictness (false-positive adoption is worst → bias to fallback); no stopword/stemming v1. spec `299f5b1`→plan `3222ac2` (both fresh-session reviewed, 5 notes folded incl. ellipsis test). Built in an **isolated git worktree** parallel with job-resilience (verified file-disjoint), 2-task subagent-driven, controller stress-tested each commit. Commits `4c80017`·`17fc0b5`; merged `--no-ff`→`6a2a361`; combined suite **231 + 1 pre-existing red**; worktree removed. Handoff `docs/handoff-notion-archive-validation.md` for the validator session. NEXT = Notion anchor auto-resolve (WISHLIST, separate) |
| 0033 | 2026-06-05 | Nggaev-v2 | **Fetch From Notion -- alternate textbook source (SHIPPED + live-verified).** New Session source chooser (Fetch From Notion / Upload). Fetch crawls `Lessons` tree grade -> Uzbek `N - sinf` -> subject, downloads the attached textbook PDF, joins the existing upload->TOC->generate path. sinf-only v1, keep-7-subjects (rest disabled), reject >20MB. Shared `ingest_pdf` refactor (both upload return shapes); new `notion_fetch.py` (map/crawl/download); `/notion` read-only router (auth) + `POST /books/from-notion`; `notion_lessons_root` setting; `get_page_title` wrapper. FE types+api+`upload.tsx` wizard. spec `d61ad0b`->plan `35bd683`(+`edb16c3` 3 reviews); 10 tasks subagent-driven, controller stress-tested each commit. **Acceptance gate caught a real bug:** Notion S3 URLs are presigned **GET-only** -> the HEAD pre-check 403'd; fixed to a streaming GET (`3e8687e`). LIVE PASS: Kimyo 3.84MB valid `%PDF-`; Ingliz-tili 54.4MB rejected. **Product finding: ~43% (20/47) of textbooks >20MB** (one 497MB) -> subset-TOC/auto-shrink is the top follow-on. Suite **268 + 1 pre-existing red**; tsc 0 + build OK. Commits b83f790..3e8687e (11). NEXT = subset-TOC/shrink for >20MB |
| 0034 | 2026-06-05 | Nggaev-v2 | **TOC bugs — section-reshuffle fix (SHIPPED) + garbled-heading root-cause.** Book `54cd9ff3` Kimyo-8. **Bug A (shipped `86bb0bd`):** sections reshuffle on refresh — `book.toc_entries` relationship had no `order_by` → unordered heap reads drift after any row update; fix = `order_by="TOCEntry.order_index"` (one line, covers REST+SSE), introspection regression test, suite 279+1 red. **Bug B (root-caused, tracked in R10, NOT fixed):** garbled chapter headings — broken-cmap decorative heading font → pypdf extracts case-scrambled+space-injected text → fed to gemini as authoritative → verbatim in `chapter_title`; clean `§` sections unaffected; frontend faithful. 2nd manifestation of R10 that breaks its printable-ratio heuristic; real fix is prompt-level (prefer native vision for titles) + needs a gemini re-extract smoke → deferred. NEXT = R10 prompt fix when prioritized |
| 0035 | 2026-06-05 | Nggaev-v2 | **Extract robustness — local-text injection + deterministic gates + failover (SHIPPED + live-verified).** Per-lesson `extract` no longer silently produces garbage. Root (R12): gemini-cli `read_file` now honors gitignore → `var/source.pdf` unreadable → flash returned `rc=0` + a **275-char refusal** that looked like success → poisoned every downstream phase (job "done" 9/9 garbage, caught only by judge). Fix: read WHOLE book text **locally** via pypdf (no CLI file-read → dodges gitignore + >20MB ceiling) → model locates lesson **by title** (R2-immune) → **Gate A** (raw text, terminal: scanned/broken-font) + **Gate B** (summary, failover: 275-char refusal) → **`_run_with_failover`** (claude excluded) → loud-fail. `ExtractRefusal`→`"wall"`=immediate failover. Size gate >600K chars → terminal "needs subset" (read-margin so overflow is detectable — plan-review blocker). Cache `v1`→`v2`. Files: config(4 settings)·failure_classifier·agent(5 fns + module pypdf import)·pipeline(extract branch). Old `extract_lesson_context` left unused (harmless). spec `2026-06-05-extract-robustness-design.md`(2 reviews)→plan(+size-gate blocker fix); 6 TDD tasks subagent-driven, controller stress-tested each commit. Suite **280 + 1 pre-existing red**. Commits `16be36a`·`97ccba5`·`be45e5a`·`47c4345`·`4431f99`·`dc48ea0`. **ACCEPTANCE: real gemini smoke** on the garbage book/lesson (`41aec815`, Franklar davlati §2-3): local read 227K chars `attachments=0`, Gate A PASS, gemini-flash → **1,544-char real summary** (Xlodvig/Merovinglar/496-yil) NOT a refusal, Gate B PASS. R12(b)(c) FIXED, (a) mitigated; R10 extract-phase glyph caught by Gate A but TOC manifestation OPEN. NEXT = subset-TOC/shrink (>20MB, ~43%); local-text for TOC (`extract-1`) |
| 0036 | 2026-06-05 | Nggaev-v2 | **TOC garbled-heading fix — de-garble in `extract_toc` prompt (SHIPPED).** Closes the case-scramble half of R10's TOC manifestation (Bug B from 0034). Broken-cmap heading fonts → pypdf extracts titles case-scrambled+space-injected (`KUr SININg e Ng MUh IM`→`KURSINING ENG MUHIM`) → fed to gemini verbatim → `chapter_title`. Fix = one **general, conditional** clause in the `extract_toc` instruction: reconstruct ONLY scrambled titles, leave clean ones as-printed. Brainstormed against shipped 0035 (local-text, no model-reads-PDF) → chose prompt de-garble (option B) over vision/staging (A); both proven live. Controller-direct TDD (small-verified-fix rule); structural guard test. **Acceptance: real gemini `extract_toc` smoke** on `54cd9ff3` — v1 over-normalized clean sections → tightened → v2: 4 headings clean, sections byte-for-byte unchanged. Suite 280+1 red. Commit `1fde640`. Forward-only. **Limit:** handles scrambled-but-readable (any book); does NOT recover glyph-ID `/Gxx` corruption (letters gone) — R10 glyph tail + `extract-1` stay OPEN |
| 0037 | 2026-06-05 | Nggaev-v2 | **LLM Phase Validator — self-verifying judge replaces the deterministic rule engine (SHIPPED + live-verified).** Every content phase is now graded by an LLM judge against its own prompt contract (`get_prompt`); the warn-only `phase_validator` (0028) is **retired/deleted**. Prompt-derived · single-call **cite-then-refute** (anti-hallucination) · **retry-then-warn** (one regen, cited failures fed back; never blocks; extract-exempt; errors → `judge-unavailable`). T1 `model_tiers` (`83f9ffa`); T2 `run_phase` `operation` param → `judge:<phase>` attribution (`d4c72a7`); T3 `phase_judge` (`0478fb8`→`b93fea1`, review caught a never-raise bug); T4 wire into `_execute_phase` keyed off `produced_by`, **guarded** regen via `_run_with_failover`, retire `phase_validator` (`651961d`). Post-plan (controller-direct, each real-smoke-verified): log cited reasons (`98e75eb`); **judge → configurable, default `claude-opus-4-7`** (`50174dd`; was non-claude tier-up; quota-flagged, revertible via `JUDGE_*`; `_SELF_FALLBACK`→gemini; fixed failover-to-gemini unjudged); flashcards drop rigid front/back word-count **minimums** (`def1d66`); **severity gate** — judge tags `major|minor`, **regen ONLY on `major`**, minor nits warn-only (`67d7882`; opus-classification verified). Codex confirmed installed (`codex.exe`) + working through the agent — only the server's stale PATH blocked it (restart fixes; no code change). spec→plan fresh-session reviewed (2 plan bugs fixed pre-build); T1–T4 subagent-driven + controller-stress-tested; T3/T4 code-quality reviewed. **LIVE:** citation-backed catches, 0 hallucinations, placeholder rule both ways, e2e regen on real jobs `ec3c52d5`/`927b664d` (4 substantive flashcard issues → regen → done). Suite **281 + 1 pre-existing red**. **Deferred:** Notion archive validator (spec `78b0c73` + plan `9aad1ae`, NOT executed, parked in WISHLIST). **⚠ restart** for judge=opus + codex + softened prompt. NEXT = watch opus claude-Max draw; infra-vs-content warning split (WISHLIST) |
| 0038 | 2026-06-05 | Nggaev-v2 | **Cancel Generation — kill running job + its CLI process tree (SHIPPED + live-verified).** Operator cancels a queued/running job; running cancel kills every provider-CLI subprocess + children, unwinds the pipeline, marks `cancelled`. Hard-stop: discard in-flight phase, **preserve done phases** (so /retry resumes). Core (review found it): cancellation didn't cascade — `_run_content_phases_parallel` `await asyncio.wait` doesn't cancel awaitables + existing peer-cancel is `except Exception` (can't catch `CancelledError`/BaseException), and `_spawn`'s kill was top-process-only. Built: **C1** scheduler cancel-cascade (`81bd644`), **C2** psutil `kill_tree` (`a6d6e49`, new dep) wired into `_spawn` (`406e357`); repo helpers (`64989e6`); `RUNNING_JOBS` registry (`a29aa91`); heartbeat self-cancel (`935e337`); **shielded discriminating finalize** uncancel+shield (`0b55e84`); stale-`cancelling` sweep (`a166e6a`); `POST /jobs/{id}/cancel` (`bb8ba16`); `/retry`-allows-cancelled-as-resume (`c1fa229`); FE Cancel button (`cb27933`). No migration (status free string). spec `b5eac84`→plan `22a0b84` reviewed 3× (toc_entry_id stub fix MUST; uncancel hardening). 11 tasks subagent-driven, controller stress-tested each commit. **LIVE: real gemini CLI 3-process tree (gemini.CMD→node→child) reaped on cancel, 0 orphans.** Suite 299+1 red; tsc 0 + build OK. NEXT = single full e2e live run + queue/resume paths; restart server to expose endpoint |
| 0039 | 2026-06-05 | Nggaev-v2 | **SSE session leak fix.** SQLAlchemy "non-checked-in connection" GC warning (live run) → both SSE generators (`jobs.stream_job`, `books.stream_toc`) yielded initial-state events while inside `async with SessionLocal()`, holding a pooled connection across the suspension → abrupt client disconnect GC's the async-gen without clean `aclose()` → orphaned connection. Hunt verified all `SessionLocal()` are context-managed + the TOC `create_task` already retained (`_TOC_TASKS`, WISHLIST `:80` stale). Fix: snapshot inside block → release → yield (order preserved). AST regression test (`test_sse_session_no_yield.py`) asserts no yield inside a SessionLocal block. Controller-direct TDD (small-clear-fix). Suite 301+1 red; commit `2f56136`. Candidate-not-proven caveat noted; fix is correct hygiene regardless |
| 0040 | 2026-06-06 | Nggaev-v2 | **Gate A false-positive fix (0035 regression).** Readable math/science books terminal-failed at `extract` — `validate_extract_text` computed letter ratio over `len(stripped)` (only outer-trimmed), so internal layout whitespace + digits + symbols sank a real algebra book (`62865c70`, 27.6MB) to 0.44 < 0.55 floor. Fix: divide by VISIBLE (non-whitespace) chars; threshold unchanged. Verified: scanned all 12 books (only algebra wrongly rejected, 0.444→0.680; rest pass wider); garbage still rejects (glyph 0.25, CID 0.375) — clean band real≥0.68 / garbage≤0.375. Regression test in `test_extract_gates.py`. Suite 302+1 red. Log's other error = cosmetic SSE teardown from [[0039]] (needs restart) |
| 0041 | 2026-06-06 | Nggaev-v2 | **Cancel UI dead-end + dead-bus stream hang — closes client/stream gaps in 0038 (SHIPPED).** 2nd reviewer found two FE/SSE holes (0038 backend sound). (1) Cancelling a *running* job dead-ended on the `cancelling` badge + frozen "Running" rows until manual reload — never reached the `cancelled`/Resume panel. (2) A fresh stream for an already-cancelled/cancelling job hung on a dead bus. Root cause (re-verified live): cancellation publishes **no terminal SSE event** — `pipeline.run` `finally: events_bus.close()` (`pipeline.py:252-253`) closes the bus DURING unwind, BEFORE `_execute_job`'s `except CancelledError` → `uncancel()` → `shield(mark_cancelled)` commits `cancelled` (`worker.py:215-241`). So refetch-on-close **races the commit** (reviewer-caught, re-stuck `cancelling`); a `job_cancelled` event in `mark_cancelled` is DOA (bus closed). Fix: FE `job.tsx` **polls getJob to terminal** while `status==="cancelling"` (race-free, covers mid-cancel reload); API `stream_job` initial-replay returns terminal `job_cancelled` for cancelling/cancelled instead of subscribing to a dead bus. Behavioral test drives `event_gen` (DB mocked) → yields `job_cancelled`, never subscribes; 0039 AST guard green; tsc clean. Suite **304+1 red**. Commit `9b87b51`. ⚠ restart to deploy |
| 0042 | 2026-06-06 | Nggaev-v2 | **Book grade derivation + silent-skip visibility — close the unarchived-job root cause (SHIPPED + live-verified).** Job `3b026cfb` (math-algebra) never archived because its book had `grade=NULL` → archive's `{subject}|{grade}` key short-circuits (`_resolve_subject_page_id` `:58-59`) → silent skip. Leak = upload path (`grade=Form(default=None)`). Verified `book.grade` is read ONLY by the archive (generation levels CEFR from the PDF source, `prompts.py:34-39`), so NULL grade made no bad content — only blocked archiving. Full pipeline (spec `e9cdeb2`, plan `9b0094f`, both reviewer-verified). Commit 1: `derive_grade_from_filename` (regex sinf/klass/класс, clamp 1–11) wired into `ingest_pdf` (explicit wins) + Alembic backfill `0020`. Commit 2: `notion_skip_reason` column `0021` + `set_notion_skip_reason`/clear-on-archive + `archive_job` stamps resolvable skips **with in-block commit** (first session is read-only — load-bearing flag) leaving disabled/idempotent untouched + optional `JobOut` field + job-page indicator + `scripts/rearchive_unarchived.py`. 8 TDD commits inline; manual-confirm gate on the 3 outward-facing steps. LIVE: migrations applied (DB at `a7c1e9d2b4f8`, only 0020+0021 ran), backfill algebra→7/english→8 (0 gradeless left), sweep archived **all 6** done+unarchived (incl. history|7 via jahon-keyword) — **all 19 done jobs `archived=t`, 0 skips**. Suite **321+1 red**. ⚠ restart server. |
| 0043 | 2026-06-06 | Nggaev-v2 | **Notion-fetch size cap 20→50MB, tied to upload cap.** `notion_fetch.download_textbook` had a separate hardcoded `_TOC_MAX_BYTES=20MB` while uploads accept 50MB — artificial (TOC handles >20MB via local pypdf text). Removed the constant; reject against `settings.max_file_mb` (dynamic, single source of truth, can't drift). Unlocks most of the ~43% over-20MB corpus for fetch (big-from-images books). Controller-direct TDD: new `test_download_accepts_above_old_20mb_cap` (30MB passes, red→green) + oversize test retargeted + `test_from_notion` de-staled. Suite 322+1 red. NOT "all books" — scanned (OCR) + text-massive (subset-TOC, `fetch-1`) + >50MB RAM (streaming upload) still walls. Logged WISHLIST `deadcode-1` (dead `source_map_*` plumbing from Effort-A [0028]). Files: notion_fetch.py + 2 tests |
| 0044 | 2026-06-06 | Nggaev-v2 | **Phantom gemini-3.5-flash removed + model-not-found → immediate failover.** `gemini-3.5-flash` was in MODEL_MANIFEST but the gemini CLI rejects it (`ModelNotFoundError`, verified live via a real job + a direct CLI sweep of all 7 gemini models — only 3.5-flash MISSING). Users could pick it → every gemini phase failed → failover salvaged but wasted ~15-30s/phase. Removed the 1 line. Also: classifier now treats model-not-found as `"wall"` (0 same-provider retries → immediate failover; a non-existent model can't succeed on retry). Caught + fixed 2 tests that used ModelNotFoundError as a generic "hard" placeholder; added regression guards (unit + driver). NOT a CLI-version issue (0.45.0; patch-only update; models are server-side). Controller-direct TDD, suite 326+1 red. Commit `55c05cc`, NOT pushed. Follow-up: manifest has no anti-phantom guard (probe-against-CLI check) |
| 0045 | 2026-06-06 | Nggaev-v2 | **Usage page redesign ("Control Center") + per-model breakdown (SHIPPED — backend live-verified; FE needs restart).** `/usage` was unreadable (windows crammed per card, dense mono, hardcoded 4 providers → opencode hidden). Brainstorm→spec→plan via visual companion (3 mockup rounds → Control Center). Scope (user): calls-per-window basis, FE re-present; **rejected $ cost + token budgets** (real cost/quota unobtainable from CLIs; Claude-Max/Kimi-0-tok make $ meaningless) — only add = **per-model breakdown**; reuse app's **Geist** font (no new fonts). Backend (additive, no migration): `stats_by_provider_model` (GROUP BY provider,model_name) + `get_agent_stats` nests `models[]` w/ per-model `success_pct` computed from success_count + NULL→`(default)` + fixed "four CLIs"→five comment. FE rewrite `usage.tsx`: segmented window control, 2-col **frosted-glass tile grid** over gradient mesh, **Apple activity-ring** headroom (per-provider gradient, tone-override amber≥80%/red>100%, aria-label), busiest provider auto-expands to **full-width hero** w/ per-model table, **Input/Cached/Output** pills + legend (renamed for clarity), dynamic providers (opencode shows), Kimi no-tokens note, dim degraded states, `tile-rise` keyframe in globals.css. 4 tasks TDD inline. LIVE-VERIFIED real DB: 5 providers incl opencode, real per-model success_pct (claude sonnet-4-6 215@97.7%). Suite **328+1 red**; tsc + build OK. ⚠ restart uvicorn to serve `models[]`. NOT pushed |
| 0046 | 2026-06-06 | Nggaev-v2 | **Usage "Space Dashboard" final design + real time-series sparklines (SHIPPED + pushed).** Iterated [[0045]]'s look with the user (visual companion + new `apple-design` skill) → polished dark space dashboard; SUPERSEDES 0045's visuals (data layer kept). Lesson: clean→wrong (wanted rich), light→wrong (wanted dark); user then gave finished `~/Downloads/{usage,layout}.tsx` + a concrete mockup → **treat a supplied mockup as the spec, stop guessing**. Final `usage.tsx` (dark-only, hardcoded white/x glass): navy starfield+purple/blue aurora, rocket hero, window pills, **4 summary cards each w/ a real SVG sparkline**, **5-across provider row** (ring + Input/Cached/Output + Active/Idle, click-selects), **separate full-width detail panel** (big ring + Calls/Success-bar/Runtime/Limit + Model Breakdown table w/ green bars). `layout.tsx`: floating glass header (all pages) + route-aware 1200px + decorative Moon button (user chose dark-only). Ring overflow fixed (64→84px + shrunk inner label). **NEW backend `series_by_window(since,now,buckets=12)`** buckets agent_usages in Python → 12-pt arrays {calls,tokens,duration_secs,success_pct}; `get_agent_stats` adds top-level `series`; FE `UsageSeries` type. Unit-tested. Suite **329+1 red**; tsc+build OK. ⚠ uvicorn restart for `series` (else sparklines empty). Commits `7738b18`/`fa909eb`/`fd5e7b5` + series backend, pushed. `.theme-light` in globals.css now dead/unused |
| 0047 | 2026-06-08 | feat/autonomous-fleet-engine | **Autonomous fleet Phase 0 — one DB + Dockerized worker (BUILT + container-accepted).** First fleet phase, built in an isolated worktree (NOT merged → becomes the accepted copy). Reused (not rebuilt): Dockerfile, compose worker service, WORKER_CONCURRENCY=0 gate, run_standalone, claim_next_job SKIP LOCKED, continuous sweep [[0031]]; whole-job work-stealing, no host-affinity. Built: T1 guarded real-DB contention test (`6c3008b`), T2 worker-only compose + bring-up runbook (`cde3375`), T3 smoke asserting attempts>0 not claimed_by (`5f225c7`). ACCEPTANCE: full container smoke PASS — 2 worker containers, one DB, 4/4 jobs pulled. **3 real bugs caught+fixed:** CRLF entrypoint (.gitattributes + Dockerfile sed, `63cd23e`), smoke sys.path (`63cd23e`), **psutil missing from uv.lock → image crash (`dca008a`; ⚠ also breaks prod on Nggaev-v2)**. Controller stress-tested each commit. Next: Phase 1 workers registry + liveness |
| 0048 | 2026-06-08 | feat/autonomous-fleet-engine | **Autonomous fleet Phase 1 — workers registry + liveness (BUILT + container-accepted).** Live "who's-alive" registry for the fleet. Built: T1 `workers` table + WorkerNode model + migration 0022 (`0a47f80`), T2 repo `is_online` (4 unit tests) + upsert/liveness (`aee14b5`), T3 worker registers+heartbeats on a DEDICATED asyncio task (`81ff3e7`), T4 `GET /api/v1/workers` (`83a764e`). Setting `worker_registry_stale_seconds=90`. Reclaim NOT rebuilt (already [[0031]]); no host-affinity. **🔴 review caught a real bug:** loop-top beat starves when all slots busy (loop blocks at `worker.py:107`) → busy worker falsely offline; VERIFIED vs code, fixed via dedicated task (mirrors per-job `_heartbeat`), regression test `test_busy_worker_still_heartbeats`. T4 subagent exceeded scope (controller-verified): `w.id`→`pc` test fix + auth header + pyproject session-loop-scope (non-regressing). ACCEPTANCE: container smoke PASS — API + 2 worker containers → `/workers` total=2 online=2; kill one + wait → online=1. DB-free baseline `5 failed (Notion, no .env)/329 passed/4 skipped`. Next: Phase 2 batches + Notion→TOC resolver + launcher |
| 0049 | 2026-06-08 | feat/autonomous-fleet-engine | **Autonomous fleet Phase 0.5 — clock-skew hardening (BUILT + container-accepted).** Moved all 7 worker-timing comparisons off each host's `datetime.now()` onto the head-DB clock (`func.now()`) so multi-PC clock skew can't break claiming/liveness. jobs.py T1 (`0f39f82`): claim filter+stamps, touch_claim, reclaim_stuck cutoff, mark_failed backoff, queue_depth, reclaim_stale_cancelling. workers.py T2 (`47b9a22`): upsert_heartbeat `func.now()` + `is_online` injected `now=` + list_with_liveness fetches db_now (keeps pure helper + 4 unit tests). Record-only `completed_at` left as datetime.now(). **🔴 reviewer caught `make_interval(secs=N)` → TypeError at construction (SQLAlchemy ≠ PG `=>` syntax); VERIFIED empirically → positional `make_interval(0,0,0,0,0,0,N)` binds + runs** (spots 3/4/6; supersedes spec's text-interval caveat — dynamic ints). T3 (`8927084`): dropped the [[0047]] T1 past-pinning crutch (source fix makes un-pinned T1 the skew regression guard). GATE real-DB `tests/integration/test_clock_skew.py` (4) — un-pinned immediate-claimability **10/10 deterministic**, interval-SQL runs on PG, backoff server-side-future, heartbeat DB-stamped. ACCEPTANCE: rebuilt image + 2 worker containers → smoke `4/4 pulled` via func.now() claim + both containers `online=True`. DB-free baseline `5 failed (Notion)/329 passed/8 skipped`. Severity: robustness not blocking (NTP fleet <100ms). Next: Phase 2 batches + Notion→TOC resolver + launcher |
| 0050 | 2026-06-08 | feat/autonomous-fleet-engine | **Autonomous fleet Phase 2 — batch automation (BUILT + container-accepted).** `POST /jobs/batch` fans out one job per textbook lesson into the shared queue, tracked by a `batches` row w/ a drift-free computed-on-read rollup. **FAN-OUT ONLY** (batch = label+tally, only sees `toc_ready` books; download/ingest/extract + failure stays owned by existing `/books/from-notion`→`toc_extractor`; narrows master §5, intentional). Schema T1 (`0866767`, migration `0023`/`a1b2c3d4e5f6`): `batches` w/ **UNIQUE(book_id)** (one batch per book → race-safe find-or-create + unambiguous adoption), no status counters; nullable `homework_jobs.batch_id`. T2 (`a21395f`): `jobs_repo.create(batch_id=)`. Repo T3 (`74e5dda`): `get_or_create_for_book` (ON CONFLICT … RETURNING), `rollup_for_batch` **per-lesson-latest** (DISTINCT ON toc_entry_id scoped to batch_id, mirrors latest_by_section) so retries count once → reconciles; **denominator derived (sum of tally), fully counter-free** (dropped interim `lessons_targeted` in review). Endpoint T4 (`d50b18c`): guard matrix (`toc_ready` only, real statuses — **T0 `e02e700` killed fake `status="ready"` seed trap**), default provider=**claude** (fleet cli-first), **atomic adopt-reconcile** (advisory-lock + find_active reuse; adopt `batch_id IS NULL` orphans, no poaching), `GET /jobs/batches[/{id}]`. **🟡 Gotcha handled:** UUIDPK/Timestamps are Python-side defaults → Core ON-CONFLICT insert needs explicit `id=uuid4()`+`func.now()` (verified `expire_on_commit=False` too). GATE real-DB: schema + repo + **endpoint cases 1–7** (incl. re-launch reconciles to 5-not-7, adopt orphan, **concurrent race→1 batch+5 jobs 3/3 deterministic**, force) + DB-free validation. ACCEPTANCE: rebuilt image + API+worker containers → `POST /jobs/batch` `jobs_created=4` → worker pulled **4/4** → API rollup `lessons_covered=4 complete=true`. DB-free baseline `5 failed (Notion)/330 passed/18 skipped`. UI=Phase 3, api-mode=Phase 4. Next: Phase 3 fleet dashboard |
| 0051 | 2026-06-08 | feat/autonomous-fleet-engine | **Autonomous fleet Phase 3 — operations dashboard (BUILT + acceptance-passed).** New `/fleet` operator screen (BARE-MINIMUM scope, user-locked): prepare a Notion subject → launch as a batch → watch per-status rollups + PC liveness → drill into lessons w/ per-lesson Cancel/Retry/Open. Pure FE + **ONE new read endpoint** `GET /jobs/batches/{id}/jobs` (T1 `b95689d`, per-lesson-latest DISTINCT-ON joined to TOCEntry → row count == rollup denominator; 404). FE: data layer+types+route+nav (T2 `4f7a6c9`), WorkerCards + launcher w/ **server-derived tray** (toc_ready ∉ any batch) + digits-only-grade `fetchBookFromNotion` prepare (T3 `4c6cdf8`), RollupBar (iterates non-zero statuses, cancelled/cancelling always show) + BatchFunnel + drill-in **gated by expand** (T4 `57f01d0`), optional subset launch from `getBook.toc` (T5 `a734ca5`). **🔴 acceptance caught a latent Phase-2 bug (`5a7e28d`):** `GET /jobs/batches` was **shadowed by dynamic `/jobs/{job_id}`** (→422, "batches" parsed as job_id) — Phase 3 funnel is its first consumer; fixed by registering `batch.router` before `jobs.router` + regression test. ACCEPTANCE: in-process real-DB endpoint click-through (dashboard touches no generation → CLI smoke unneeded) workers→launch→funnel→drill-in→cancel(per-lesson-latest rollup `{cancelled:1,pending:4}`)→subset(2 jobs) **17/17 PASS**. Gate: **20 integration pass**, DB-free `5 failed (Notion)/330/20 skipped`, FE tsc+build clean every commit. Deferred→WISHLIST (`fleet-*`). Worktree NOT merged. Next: Phase 4 (Gemini-API + cred-pool + cost-ledger) or merge the fleet worktree |
| 0052 | 2026-06-10 | feat/autonomous-fleet-engine | **R14 — CBP prompt self-conflict fix (universal first-pass MAJOR regen eliminated).** Tracked from the 4-model smoke: 5/5 first CBP attempts failed judge self-check #6 (concept leaked in setup), 3/5 via the required "self-sufficient" visual caption — a prompt-contract conflict, not a model defect; +1 content +1 Opus judge call tax on ~every lesson. Fix (`f5c4400`): setup-visual no-spoiler rule + narrative symptoms-not-diagnosis + DPE required closing line + self-checks #6-ext/#9. PROOF: in-process CLI smoke vs real England extract — gemini-3.1-pro + 2.5-pro both FIRST-PASS PASS, 0 warnings (2-iteration fix: first smoke exposed the prose leak channel). 89 prompt/judge tests green. R14 closed |
| 0053 | 2026-06-11 | feat/autonomous-fleet-engine | **Autonomous fleet Phase 4 — CLI \| API transport toggle (SHIPPED + in-band acceptance; billed smokes operator-run).** Per-job/per-batch `transport` enum (`cli` default · `api` claude+gemini only) to benchmark subscription-vs-pay-per-token mass-gen. 7 tasks (`49e2670`·`dfa3973`·`0468d62`·`7317280`·`aeb5fd8`·`21e799e`·`a200c20`): T1 `transport`/`auth_mode` cols + migration 0024, batch key→`(book_id,transport)`; T2 `_auth_env` adapter (cli baseline EVERY spawn + gemini GCA; api injects exactly the active key, loud RuntimeError on missing); T3 `validate_transport`+`api_supported` at generate/batch + §9a transport-scoped dedup (api-over-cli book makes N fresh jobs, no silent empty-batch); T4 pipeline/judge threading + **api=requested-provider-only failover** + **loud judge auth-fail on api**; T5 worker `has_api_keys` claim gate (both keys req'd) + gemini `selectedType` startup guard; T6 `auth_mode` attribution + `pricing.py` (claude VERIFIED $5/$25, **gemini `VERIFY`**) + per-transport `$`; T7 FE toggle/badge/`$` + transport-aware launcher gate. Key decisions: cli baseline for every spawn (TOC-at-upload has no job), `(book_id,transport)` key+dedup (benchmark = same book cli then api), api single-provider failover (no `model=None` $0 path), loud judge 401, claim gate. **IN-BAND ACCEPTANCE (free):** full suite `5 failed (Notion)/362 passed/35 skipped`; Phase-4 units **25 passed**; real-DB schema/claim/validation **19 passed**; **free invalid-key 401 proof** = claude CLI `api_error_status:401` "Invalid API key", `total_cost_usd:0`, `output_tokens:0`, raised RuntimeError — HITS the API, NO subscription fallback, ZERO spend, fails closed. **DEFERRED→operator:** `docs/runbooks/phase4-transport-operator-acceptance.md` (§7.1 live valid-key smokes, §3 ship-code-before-removing-`selectedType` ordering, §7.2 post-removal upload, §7.5 e2e api attribution per §9b, gemini-price VERIFY); `fleet-api-1..5` in WISHLIST. Next: operator billed gates + gemini-price verify |
| 0054 | 2026-06-11 | feat/autonomous-fleet-engine | **Autonomous fleet Phase 4.1 — per-role transports (SHIPPED + in-band acceptance incl. LIVE mixed-billing smoke).** Per-job/per-batch `extract_transport`/`judge_transport` (`cli\|api\|inherit`, default `inherit`; `resolve_role_transport` — inherit follows job, explicit wins; content always job-level) = `fleet-api-7`. 7 tasks (`ca770b5`·`87a7cf6`·`9755886`·`e750ac0`·`4a5b475`·`5e97ddf`·docs): T1 columns + migration 0025; T2 §5a **typed `AuthEnvError`** + isinstance auth-classification; T3 validation/persistence at generate+batch; T4 **claim gate v2** — `CAPABILITIES` per-side+per-role flags, gemini-only worker claims api-gemini-content+cli-judge jobs, §4a judge **self-fallback** branch, **NULL-model coalesce** (bare SQL `NULL='x'` silently unclaimed model-NULL jobs on the judge provider — proven on real PG, fixed `coalesce(model,'')` + startup warn when JUDGE_MODEL==default); T5 pipeline threads resolved transports, regen-guard loud on content\|\|judge==api; T6 FE Extract/Judge selects. **ACCEPTANCE:** DB-free `394 passed, 46 skipped`; real-DB (edu_copy) `34 passed`; **LIVE §9.3 mixed smoke** (no ANTHROPIC_API_KEY): real gemini api spawn (Vertex) + real claude cli judge spawn → rows `auth_mode=api`/`auth_mode=cli` both success — mixed attribution proven. Live find: persisted gemini `selectedType` **overrides** the Vertex env signal (fails loud, `success=False`) → runbook. **DEFERRED:** §9.4 billed opus+api-judge e2e leg (operator). Runbook + ⚠️ role transports NOT in dedup key — re-billing needs `force=true`. Next: §9.4 / remaining `fleet-api-*` |
| 0055 | 2026-06-12 | feat/autonomous-fleet-engine | **Fleet page quick fixes — grade/done badges + dead-worker prune.** `bb88670`: BookOut exposes `grade` (red-first test) + FE grade chip, x/y-done rollup, per-lesson status badges (data already flowed; render-only). `824b5fd`: append-only `workers` registry pruned — `prune_stale` (default 2h retention) in the sweep (load-bearing) + graceful-stop deregistration (bonus); claimed_by is FK-free so history safe; 2 surgical real-DB tests. Suite 405/0, tsc+build clean, head-PC re-verified. Parked: `/batches` page restructure (fleet-ui-5 amended: + workers, /fleet keeps online-strip) + sigma-designs reshape |
| 0056 | 2026-06-12 | Nggaev-v2 | **Infra consolidation + backend small-fixes.** Windows head retired → both DBs restored to native Homebrew PG16 on the Mac (localhost:5432); Mac is now fleet head. Branch: obsolete `Nggaev-v2` force-updated to fleet tip + made the working branch, `feat/autonomous-fleet-engine` deleted (default-rename needs admin). `c24f873`: VAR_DIR wired via new `storage.book_pdf_path()` helper (R13 dead-setting half done), `reclaim_stale_cancelling` rowcount-or-0, 4 terminal completed_at→func.now(). Reviewer caught commit-msg overstatement → `fleet-ops-2` (pipeline completions still host-clock, harmless). Suite 408/0. Backend audit found no other real defects. |
| 0057 | 2026-06-12 | Nggaev-v2 (PR #4) | **Chunk-3 sub-project 1 — Fleet → Monitor restructure (+ navbar-centering fix).** New `/monitor` page (WorkerCards+BatchFunnel) + `OnlineStrip` liveness (amber anti-footgun) on a launch-focused `/fleet`; Monitor nav item; actionable "View in Monitor" toast. `523ffb2`·`797cc05`·`5f279ed`·`f340e45` + `3f04a3c` (pin navbar max-w-[1200px] — was route-dependent, slid on wide pages). FE-only, 408/0 backend untouched, clickthrough PASS (Playwright). Spec+plan → shipped/. Sub-project 2 (sigma-designs) open. Sibling: PR #5 download-token fix (separate). |
| 0058 | 2026-06-12 | Nggaev-v2 (PR #5) | **Fix — homework download 401 on live completion.** `job.tsx` job_completed handler used the server's token-LESS download_url → 401 → browser saved `download.json`. Now uses token-aware `api.jobDownloadUrl(id)` like the load/poll paths. `7a1e21d`, FE 1-liner; verified live (200 w/ token, 401 w/o). |
| 0059 | 2026-06-15 | Nggaev-v2 (PR #6) | **Chunk-3 sub-project 2 — Sigma-designs reshape (foundation + fleet).** Living-glass + AI rim-glow as shared primitives (fleet = first consumer), glow-as-accent. Full gated pipeline (brainstorm→spec→reviewer-written plan→subagent-driven exec, controller stress-tested every commit). 6 commits: `3a5cdb1` globals.css foundation (`@property --ang`, rim-spin/aurora-drift/seg-pulse keyframes, `.glow-rim`/`.aurora-bloom`/`.seg-now`, Sigma `@theme` tokens, ONE reduced-motion gate); `e9cc4d3` ui.ts glass tokens = global re-skin of 6 routes + 4 fleet comps (no chip palette); `ca0e40c` drifting aurora SpaceBackdrop (CSS-gated reduced-motion, per-route kept); `c0fa59c` dead Moon button removed; `ac6f774` OnlineStrip emerald+glow; `34fd31b` monitor moments (`.glow-rim` on `!complete` BatchCard + `.seg-now` on running segment). **Glow=brand, color=semantic** — extended to OnlineStrip (emerald not cyan) + pipeline-now (blue not cyan), user-confirmed. Truths honored: SpaceBackdrop per-route (no hoist), one reduced-motion mechanism, two semantic status impls untouched, `--color-accent` amber NOT retoken'd. FE-only; gates green per commit; operator visual acceptance PASS (incl. reduced-motion freeze). Mockup `docs/design/2026-06-15-sigma-fleet-mockup.html`. Not-yet-aligned (later): navbar glass, library/usage accents, `@property` browser floor. |
| 0060 | 2026-06-15 | Nggaev-v2 (PR #7) | **Fleet launcher — launch-remaining + per-lesson retry on partially-done batches.** Two operator-caught defects: (A) launch button disabled on `alreadyBatched` not completion → dead "CLI launched" at 7/26, couldn't continue; (B) done lessons freely re-selectable in the picker. **FE-only — backend already supported both** (`find_active_for_section` includes `done` → plain re-launch skips done/in-flight, creates only the rest; `force=true` regenerates). Controller-direct (root cause verified, low-risk). `8ddaf91`+`85ffa28` (`launcher.tsx` + 1-line `api.ts` exposing backend `force`): primary button = Launch / Launch remaining N / disabled Complete / disabled In progress (+ Launch while toc loads); `Re-run all` force ghost button (confirm, notes api billing); picker done lessons non-selectable (check+"done") with per-lesson `Retry` (force, single toc id, confirm). tsc+build clean; operator behavioral verify. Parallel with PR #6 (0059) off Nggaev-v2 → trivial both-append merge conflict expected. |
| 0062 | 2026-06-16 | Nggaev-v2 (PR) | **R13 — pull-on-demand source PDFs (bytes-distribution half).** Closes R13: a fleet worker missing a book's `source.pdf` fetches it from the head over HTTP, caches it, proceeds (was: instant `extract` failure on multi-PC). Backend-only, no schema change. `2c59ce5` `fleet_head_url` config (empty=today's behavior); `f4db6d0` `GET /books/{id}/source.pdf` (FileResponse, include-auth) +3 tests; `c7cb8ee` sync `book_fetch.ensure_book_pdf_sync` (exists→fast-path; `_fetch_to_temp` seam → atomic pid-temp+`os.replace`; first-token auth) +5 tests; `8d4c538` pipeline swap via `asyncio.to_thread`. 4 robustness gaps (to_thread off-loop, mkdir parents, comma-string auth_token, Windows close-before-unlink). `6e1b7d0` incidental: 401 test made resilient to a pre-existing leaked `dependency_overrides[get_current_user]` (4 api modules never clean up → `test-hygiene-1`). Suite 416/48/0; localhost 2-process HTTP smoke passed. Operator `fleet-test-1` real 2-PC run remains. |
| 0061 | 2026-06-16 | Nggaev-v2 | **api-3 — remove dead classify/difficulty cluster (Group 1).** Picked up in parallel with R13 (chosen for zero file overlap). Found TWO difficulty clusters: **Group 1** = classify-era event/schema/column (removed — zero pipeline.py footprint); **Group 2** = `difficulty=None` param threaded through ~20 agent.py/pipeline.py sites (left for a later deadcode pass — overlaps R13). Verified dead first (`set_difficulty` 0 callers, `ClassifyDecision`/`Difficulty` unused, no test builds the model w/ difficulty). Removed backend: `difficulty_classified` SSE event, `DifficultyClassifiedEvent`, `set_difficulty`, `JobOut.difficulty`, `schemas/classify.py`, `homework_jobs.difficulty` column (**Alembic `0026_drop_difficulty`**, single head, needs `alembic upgrade head`). Removed FE: `Difficulty` type + `Job.difficulty` + `difficulty_classified` event + dead state/chip in job.tsx/preview.tsx; reworded "classify difficulty" copy (job/section/upload). Left `FlashcardDifficulty` + `[Difficulty:]` meta-tag stripping. Gates: **408/48** backend, tsc+build clean. Controller-direct; R13-independent. |
| 0063 | 2026-06-16 | Nggaev-v2 (PR #11) | **SDK-based `transport=api` (gemini google-genai + claude anthropic).** api transport now calls provider SDKs directly via new `app/services/api_transport.py` (same `(rc,text,usage,stderr)` 4-tuple as `_spawn`), dispatched in `_spawn` early-but-inside-`_semaphore()`; cli unchanged. Motivated by measured CLI-with-key tax (~2.6–10× tokens, ~1.5–3× slower, equal-quality). Loud truncation (claude max_tokens / gemini MAX_TOKENS), `settings.api_max_output_tokens=16384` (gemini uncapped), deps `google-genai`+`anthropic`, CLAUDE.md + reference docs no-SDK rule scoped to cli. 5 commits, merged `f760f26`. **422/48** + real gemini SDK smoke; claude smoke pending a key. Reference docs (README/HOW_IT_WORKS/CODE_MAP) de-staled. Numbered 0063 — `0062` held by R13's unmerged branch. |
| 0064 | 2026-06-16 | Nggaev-v2 (PR) | **Gemini 4-model api run + single-PC max-concurrency research.** Autonomous: generated 4 homeworks (one lesson, 4 gemini models, `transport=api`/Vertex SA) → `~/Documents/`; stress-tested single-PC concurrency. Verified all 4 `done` (37 api content rows + 42 cli extract/judge rows; **$1.27** real spend). Gotcha: api jobs need `extract_transport=cli`+`judge_transport=cli` on a no-`ANTHROPIC_API_KEY` worker (judge/extract roles + text-only api). **Ceiling:** PC not the bottleneck (~25% CPU @ N=64); Vertex per-model quota is — ~50–64 concurrent flash calls; ≈**16–21 concurrent flash-tier homeworks** (fan-out 3). Harness scripts + `docs/research/2026-06-16-gemini-api-concurrency.md`; stress probe cost $0.0009 (no homework spam). |
| 0065 | 2026-06-16 | feat/all-subjects-prompts → Nggaev-v2 | **All-subjects support — single subject registry (P1).** New `app/services/subjects.py` (`SubjectDef`+`REGISTRY`, 26 Uzbek-curriculum codes — academic subjects + textbook-bearing non-exam ones; PE + 3 textbook-less soft subjects excluded after a Notion textbook audit) as the source of truth; `flows`/`prompts`/`notion_fetch`/FE all derive from it. Key finding: the prompt layer was **already** family/language-general (`FAMILY_RULES`+`_default`, graceful unknown-subject fallback) — this was a registry job, not a prompt rewrite. Added Russian-L2 rule. 7 legacy codes preserved (regression test); books.py unchanged (derives `SUPPORTED_SUBJECTS`); no migration. **617/48**, tsc clean; real claude-CLI smoke on 2 new subjects (geografiya, russian) passed. Unblocks the Oct/Mar campaign's P1 ([[oct-mar-content-campaign]]). |
| 0066 | 2026-06-17 | Nggaev-v2 (PR #18) | **Three fleet-robustness fixes (0-entry TOC, R13 cache integrity, failover-skip).** From the live 2-PC fleet test; closed 3 WISHLIST items. (1) `toc-empty-ready`: `toc_extractor.run` raises on 0 entries → `failed`+error SSE (scanned/empty PDF no longer silent `toc_ready`). (2) `r13-integrity-1`: `ensure_book_pdf_sync(book_id, expected_size)` drops a wrong-size cache (only when a head exists to re-fetch) + rejects truncated downloads (stops `pypdf: Cannot find Root object` on a corrupt cache). (3) `fleet-failover-1`: `agent.provider_cli_installed` → `_run_with_failover` skips uninstalled fallback CLIs (never the requested). Backend-only, **625/48**, 3 new test files. **A 4th fix reverted** (`b3e0772`, extract→cli coercion — wrong premise; extract is text-inline `attachments=[]`, runs fine via api). Deferred to next PR: subset-extraction + scanned-PDF OCR features ([[extract-robustness-feature-design]]). Worklog filed as a follow-up (PR #18 shipped without it). |
| 0067 | 2026-06-17 | feat/all-games → Nggaev-v2 | **Generate all 7 Gamified Practices every job (8→11 phases).** `flow_for` now emits all four interaction mini-games (memory-match/tictactoe/jigsaw/sentence) every job, not just the one `SUBJECT_GAME` pick → the full gamified set (those 4 + rlc + error-detection + boss-arena) generates, none skipped; curation of "which fits" is downstream. One functional file (`flows.py`); `SUBJECT_GAME` kept as a recommendation-only hint. No FE/pipeline/PHASE_DEPS/prompt/DB change (verified). Gated pipeline (spec→plan→subagent exec, controller-verified). **703/48**; real claude smoke of practice-jigsaw/biology (a newly-generated game) passed. Cost ≈+37% phases but less in $; fleet watch-item: wider per-job wave ([[api-job-claim-gate-and-concurrency]]). |
| 0068 | 2026-06-17 | feat/per-role-exec → Nggaev-v2 (PR #20) | **Per-role provider/model selection + API-default generator.** A job pins separate provider/model per role — generator, `extract`, `judge` — not one provider for all. Migration `0027` (per-role columns on jobs+batches); per-role API validation; extract resolved per-job (settings fallback); cross-job extract reuse re-keyed on provider+model; `/agent/models` per-model tier; FE role controls + API-default generator + weak-judge warning. **Self-grade guard** (`model_tiers.resolve_judge`): judge override hard-swapped to auto-tier if it resolves to the generator's model. **Blocker fixed pre-merge** (`455d238`): raw `judge_model=None` slipped past → now both sides resolved before compare, fallback `judge_model_for` (not `_SELF_FALLBACK`, which would self-match gemini-3.1-pro) — closes the reviewer's optional edge too. **719/51** on the base-merged branch; tsc clean; per-role smoke passes. Orphan `feat/per-role-provider-model` retired. |
| 0069 | 2026-06-17 | Habibullo → Nggaev-v2 | **UI/UX redesign + subject-category drill-down.** New generic nestable `web/src/components/category-browser.tsx`; Library + Fleet now front their cards with a **two-level grade → subject drill-down**; the Fleet per-book launcher became a collapsible card. The redesign (single commit `cbdb601`, forked at PR #17) was **rebased onto the current Nggaev-v2 tip (`4d9ffc9`)** — one conflict (`launcher.tsx`), reconciled to keep **both** the redesign layout **and** PR #20's per-role `RoleAgentControls`/API-default/weak-judge warning (dead `RoleTransportSelect` removed). Touch-ups: `aria-expanded` on the per-book toggle; drill-down spec synced to shipped reality (`getGroupKey`/`groupLabel`, nested grade→subject) + `git mv` to `shipped/`. FE-only. tsc clean; vite build OK; structural scan + **Playwright** review (upload/library/fleet + drilled launcher, **zero console/runtime errors**). |
| 0070 | 2026-06-17 | feat/extract-robustness → Nggaev-v2 | **Extract robustness — scanned-PDF vision + oversize text subset.** Two fallbacks in the lesson-extract branch; normal whole-text path 100% unchanged. **Oversize** (whole text > `extract_max_text_chars=600K`, e.g. adabiyot-g9 665K) → scope to the lesson's pages as text (`read_page_range_text`) → existing `summarize_lesson` (keeps `transport=api`). **Scanned/sparse** → vision-attach a page-window PDF (`summarize_lesson_vision`, **forced cli** — api can't attach) and find-by-title within the window. **Density gate caught by the smoke:** Gate A's absolute 500-char floor MISSES a 240pg×71-char/pg scan → added `extract_text_is_too_sparse(text,n_pages)` + `pdf_page_count` + `extract_min_chars_per_page=300` (clean separation across all 16 books). Window `±5`/cap-25, find-by-title absorbs page-offset; missing pages → fail loud. **741/51**; **real-cli acceptance smoke** (claude cli, gemini-cli OAuth absent on the Mac; zero DB writes): scanned→accurate §5 summary, oversize→real "Alisher Navoiy" summary (both GATE-B PASS). Lesson-extract only; scanned-TOC (line 79) stays open. Closes `fetch-1`(b) + scanned-lesson half of `fetch-1`(c). |
| 0071 | 2026-06-17 | fix/fleet-launcher-ux → Nggaev-v2 (PR #24) | **Fleet launcher UX fixes (FE-only).** (1) Per-role **Model dropdown** now renders only once a concrete provider is chosen — was rendered-but-`disabled` on "Auto" (two dead dropdowns); fixes launcher + section (shared `RoleAgentControls`). (2) **Launched book no longer vanishes** from the Fleet — removed the `!fullyBatched` tray exclusion (a book batched on both transports got dropped; API-default made it trivial to hit; pre-existing bug). (3) **Honest status** instead of always "ready": caption/sections = ready vs launched (batches only); per-book card chip = ready to launch / generating / complete from its own TOC — `complete` needs every lesson done, `generating` needs `activeCount > 0` (failed/idle no longer falsely pulses generating). tsc clean, vite build OK, Playwright zero console/runtime errors. Fast-follow: tray grows monotonically — a "Complete" split needs per-book progress at list level (backend), deferred. |
| 0072 | 2026-06-17 | feat/scanned-toc-vision → Nggaev-v2 | **Scanned-TOC vision fallback** (upstream sibling of 0070). In `agent.extract_toc`: when the local TOC text is empty OR `extract_text_is_too_sparse` (watermark-only scan), drop the junk text and **vision-attach a bounded front+back page-window** (new `_toc_source_pdf`) so the provider OCRs the contents page (Mundarija prints front OR back). Dense-text path byte-identical (`else`). Window **configurable+asymmetric** (`extract_toc_front_pages=12`/`extract_toc_back_pages=20`) → too-narrow remedy is "bump env + re-extract". **Actionable 0-entry fail** (names cause+widen remedy). Forced-cli automatic (TOC = upload, no job). **749/51**; **real claude-cli acceptance** on `a0173601` (was 0 entries) → **39 real entries** with titles+pages from the back-of-book TOC. Closes the scanned-TOC half (WISHLIST line 79) — last open piece of the scanned-book story. |
| 0073 | 2026-06-18 | feat/toc-retry → Nggaev-v2 | **Retry book TOC extraction.** New `POST /books/{id}/toc/retry` (mirrors job-retry) + FE **Retry** button on failed/stuck books — re-prepares a book whose TOC extraction failed (e.g. the `a0173601` claude-rate-limit case) without delete+re-upload. Retry from `failed`/stuck `toc_extracting` only (`toc_ready`/`uploading`→409); guards 404 + PDF-missing 409; optimistic `toc_extracting` flip + stale-error clear; reuses the TOC SSE stream. DRY'd the upload/from-notion trigger into `_start_toc_extraction`. **Idempotent re-extract** (gatekeeper): `bulk_create` appends (no upsert) → added `toc_repo.delete_for_book` clear-before-insert in `toc_extractor`; FK-safe since retry excludes `toc_ready`. Also fixed `set_status` to always clear `error_message`. **756/51**, tsc+build clean. No generation impact. |
| 0074 | 2026-06-18 | feat/history-variant-label → Nggaev-v2 | **History variant label (R17).** Distinguish **Jahon tarixi** (World) vs **O'zbekiston tarixi** (national) — both rendered identically as "History · grade N". Backend-derived **non-persisted** `subject_variant` (`"jahon"|"ozbekiston"|null`) from `original_filename`, exposed on `BookOut` (Pydantic `computed_field`) + the batch rollup payload (`books` join in `list_with_rollups`); FE `subjectLabelWithVariant` → **"History · Jahon · grade N"** in batch funnel + 3 launcher cards + Library subtag. Detector reuses the **same apostrophe-fold as the archive split** (drift-guard test cross-checks `notion_archive._fold`). Notion audit: G8–11 (+G7 with mis-titled `Tarix Uzb`) carry both; live `NOTION_SUBJECT_PAGES` already routes G6–11 correctly — gap was display-only. **762/51**; tsc + build clean; no CLI smoke (display-only). **R16** + `history|5` config stay OPEN. |
| 0075 | 2026-06-18 | fix/toc-delete-collision → Nggaev-v2 | **Hotfix: `delete_for_book` name collision** (bug from 0073). `toc_entries.py` imports SQLAlchemy `delete` AND defines a public `delete(session, toc_entry_id)` that shadows it → `delete_for_book`'s `delete(TOCEntry)` raised `TypeError: missing toc_entry_id` at the clear-before-insert. **Live impact:** a real 8.5-min claude-vision re-extract of Algebra-g9 succeeded (60 entries) then crashed at persist → wasted entries + quota. Fix: `delete as sa_delete`. DB-free regression test runs the real body (proven red on the bug). Slipped 0073 because the only coverage mocked `delete_for_book` itself. **775/51.** Backend-only. |
| 0076 | 2026-06-18 | fix/safe-uuid-nonsecure → Nggaev-v2 | **Fix: `crypto.randomUUID` undefined on HTTP LAN IP.** The section-page Generate/Retry minted an idempotency key via `crypto.randomUUID()` — which is only defined in a secure context (HTTPS/localhost), so over plain HTTP on the fleet head's LAN IP it threw `TypeError: not a function` before any API call → section retry "didn't respond"/errored (Monitor-page retry worked since it reuses the job). New `web/src/lib/uuid.ts::safeUUID()` (native → `getRandomValues` v4 → Math.random fallback); `section.tsx` swapped. FE-only, tsc+build clean. Deploy = `npm run build` + hard-refresh (web/dist gitignored). Independent of the 0073 book-TOC retry. |
| 0077 | 2026-06-18 | cluster-1-hardening → Nggaev-v2 | **Cluster 1 — Quick-win hardening (8 fixes):** dep-CVE bumps (pypdf/python-multipart/starlette), flow-1 scheduler-stuck f-string, concurrency-knob (semaphore reads `agent_max_concurrency`, `gemini_*` deprecated-fallback + DEPLOY de-stale), blank-`EXTRACT_PROVIDER`→default validator, judge per-attempt timeout→judge-unavailable (not job-kill), stale-pending attempts-exhausted sweep, test-hygiene auth-override leak (5 api modules→autouse fixtures), migration `0028` CHECK constraints on status/transport enums. 791/56; pip-audit clean on the three; 44 DB-integration green (8 new constraint cases). |
| 0078 | 2026-06-18 | cluster-2-cancel-resume → Nggaev-v2 | **Cluster 2 — cancel/resume correctness.** Guarded `set_status` (cancel-race-1: terminal frozen, cancelling→only cancelled) + done-phase freeze; batch **cancel-all** + **resume** endpoints; resume-aware **relaunch** (strict zero-write preview + relaunch_mode; reuses saved phases instead of discard — fleet-relaunch-dataloss-1 deliverable changed to steer-to-resume); FE three-way card (Launch/Resume/Cancel-all + Re-run-all kebab). Real-DB integration tests; 804/5(notion). Closes cancel-race-1, fleet-ctrl-1/2, fleet-relaunch-dataloss-1, fleet-test-4. Merge before C4. |
| 0079 | 2026-06-18 | cluster-3-judge-quality → Nggaev-v2 | **Cluster 3 — Judge Quality: fidelity instruction + deterministic year-signal (judge-fidelity-1); judge_status column + regen cap (judge-softfail-1); claim gate resolves per-job judge/extract provider (judge-claimgate-1).** Key correction: source already reached the judge — gap was instructions + no deterministic check. 828/71; migration 0029. |
| 0080 | 2026-06-18 | cluster-4-cost-safety → Nggaev-v2 | **Cluster 4 — Cost Safety.** Paid-api kill-switch: BOTH caps (per-batch + fleet-day) → **pause-claim** halt (never hard-cancel). Cost ledger (`cost.py`) + budget monitor (`worker._budget_monitor`, reason-scoped) + batch-pause primitive (`batches.paused_*`, reused by C5) + fleet `budget_state` gate (batchless-safe) AND-composed into `claim_next_job`; never-pay-twice prior-$ warning; claude cache-write priced at 1.25× input (`cache_creation_tokens`, migrations 0030–0032) with per-provider semantics intact. Closes fleet-api-3/4, pricing-1. 912 offline + 102 real-PG integration green. |
| 0083 | 2026-06-19 | Habibullo (PR #37) → Nggaev-v2 | **PR #37 integration.** Adopt contributor's custom-prompt upload + per-job phase-picker + monitor redesign (per-transport `TransportRow`, whole-book `not_started` rollup, subject drilldown), rebased onto C1–C4. Grafted C4 budget-pause UI/payload into the rewritten files (survived 3-way merge; locked w/ test); re-chained migrations linearly → `0033_custom_prompts_phases` + `0034_widen_prompt_hash` onto `0032` (single head; caught the VARCHAR(32) revision-ID limit). Fixed 2 latent PR bugs: ON-CONFLICT NULL-out on plain re-launch (conditional `set_` — COALESCE fails on JSONB JSON-null) + vacuous inspect-grep judge-override test → behavioral. `claim_next_job`/`_fidelity_flags` byte-unchanged (C3+C4 intact). 943 offline + 65 integration green; tsc+build clean. Force-pushed to PR #37; routed to gatekeeper. |
| 0081 | 2026-06-19 | cluster-5-fleet-scale → Nggaev-v2 | **Cluster 5 / P1 — Fleet control & safety** (1st of 3; P2 token-bucket + P3 SSE follow). Peer-aware startup reclaim (`reclaim_orphans_on_startup` + `has_live_workers` — reset-all only when no live peer, else lease window; fleet-restart-reclaim-1); manual batch pause/unpause endpoints + FE (reuse C4 `pause_batch`, reason `"manual"`, clobber-proof vs budget monitor; fleet-ctrl-3); PC drain (`POST /workers/{pc_id}/drain` → `status=draining`, worker self-drains on its registry beat without clobbering; fleet-ctrl-4). No migration; 1042 passed/5 notion-env; real-PG integration RED-proved. Routed to gatekeeper. |
| 0084 | 2026-06-25 | Nggaev-v2 | **Pre-mass-gen de-risk — log hygiene + api-error capture + reactive 429 backoff.** `log-hygiene-1` (`b10f12c`): dated log format + daily `var/server.log` rotation. `api-error-capture-1` (`a84f4c9`): `agent._spawn_failure_message` records the real transport-aware error into `agent_usages.error_message` + `raw_envelope["error"]`. `concurrency-knob-1` **Phase 1** (`90b714d`/`c064040`/`b2be9aa`): `agent._spawn` is now a retry-on-429 wrapper over the unchanged `_spawn_once` (exp backoff + jitter, `RATE_LIMIT_*` config, sleep holds no slot); Phase 2 token-bucket stays OPEN. No migration; 2 env-driven judge-config test reds = config-not-defect. Reference docs de-staled (README/HOW_IT_WORKS/CODE_MAP/DEPLOY). |
| 0085 | 2026-06-26 | launcher-capability-gate → Nggaev-v2 | **Cluster 5 slice 1 — launcher capability gate (`launcher-capability-gate-1`).** Launcher greys out provider/transport picks the live fleet can't serve (was: such jobs launched → stuck `pending` forever). Workers publish a `{cli,api}` capability blob on heartbeat (`workers.capabilities` JSONB, migration 0035; `CAPABILITY_BLOB` shares `_api_capable` with claim-gate `CAPABILITIES`, no-drift test); head unions over ONLINE workers (`aggregate_fleet_capability`, fail-open at zero); `/agent/models` gains a `fleet` block; FE `serveability` helpers grey unservable provider/transport (label-suffix) + offline banner + 5s refetch + reset guards, fail-open when offline; per-role `inherit` resolves to job transport. **Claim gate untouched.** 994 offline + 15 real-PG green; 1 acceptance regression fixed (`87580c0`). Single head 0035. Routed to gatekeeper. |
| 0086 | 2026-06-26 | feat/c6-notion-archive-retry → Nggaev-v2 | **Cluster 6 — `notion-archive-1` (R15): visible + recoverable Notion archive failures.** Push wrapped in `_push_with_retry` (3× backoff) → `_record_skip` persists `notion_skip_reason="push error: <Type>"` on exhaustion (best-effort, never raises); outer catch-all records `"archive error"`. New `POST /jobs/{id}/retry-archive` (done + not-yet-archived; `expire_all` before re-read) + FE "Retry archive" button. **Rest of C6 deferred (stale premise):** map now has 38 entries + unmapped already visible → R16/crawl/validator/fe-redesign correctly not done. SDD + opus whole-branch review; caught+fixed a vacuous `expire_all` guard-test. No migration; 977 passed/99 skipped; tsc+build clean. Closes R15. |
| 0087 | 2026-06-26 | cluster-7-judge-quality → Nggaev-v2 | **Cluster 7 — judge-quality residuals (all 3 shipped).** `judge-self-fallback-1` via **Option B**: a generator-aware `_self_fallback` (peers claude-opus-4-7 / gemini-3.1-pro-preview, returns whichever ≠ generator) — provably non-self for ANY generator, not a fixed constant; also fixed `worker._compute_capabilities` (read the removed `_SELF_FALLBACK` at import) so `judge_fallback_api_ok` tracks `_self_fallback(judge_pair)`. `judge-refusal-1`: `_is_refusal` + `JudgeOutcome.refused` → `judge_status="refused"`, skips the retry-once. FE/de-comingle: `judge_status` serialized on `PhaseOut` + preview chip; infra warnings no longer co-mingle into `validation_warnings`. SDD; controller stress-tested each commit; subagent correctly stopped at the worker.py import-crash. Rebased past #41 (hand-merged `_compute_capabilities`) + #42. No migration; **1018 passed / 0 failed**; tsc clean; real Vertex judge smoke PASS. The 2 known `test_judge_resolution` reds now GREEN. |
| 0088 | 2026-06-26 | cluster-8-book-ingestion → Nggaev-v2 | **Cluster 8 (slice) — Notion-fetch correctness.** `fetch-2`: `_first_pdf_block` now prefers the textbook (`darslik`) over the workbook (`ish daftari`) via `_pdf_rank`/`_pdf_name` `min((rank,page_order))` — fixes a silent whole-subject content-quality failure where a workbook-first page became the batch's textbook (preference, not exclusion). `r13-fetch-1`: per-`book_id` `threading.Lock` in `ensure_book_pdf_sync` so N concurrent same-book lessons download once (RED-proved 5→1); all prior behavior preserved; `_cached_ok` tolerates concurrent-unlink TOCTOU. Cross-task: rewrote an out-of-diff test the lock obsoleted. **Deferred by user:** `fetch-1` (>50MB giants) + `extract-1`/R10 (glyph-loss) → operator workaround. SDD + opus whole-branch review CLEAN. No migration; 1020 passed / 5 pre-existing notion-503 env. |
| 0089 | 2026-06-26 | cluster-5-fleet-resilience → Nggaev-v2 | **Cluster 5 (resilience trio) — limited-worker backoff + session-limit autopause + net retry.** `fleet-net-1` code-half: `agent._spawn` retries transient DNS/connection errors (auth/MAX_TOKENS/session-limit stay terminal). Session-limit classify+parse (`is_session_limit` keyed on the `resets <time>` clause — robust to the `_WALL` "usage limit" overlap; `parse_session_limit_reset`→next-occurrence UTC). Per-batch `session_limit_strategy` (mig 0036, env default + resolver + launcher control): **pause** = requeue (no attempt burned) + whole-worker self-cooldown until reset (auto-resume); **switch** = fail over off claude down the chain. `_run_with_failover` checks `is_session_limit` BEFORE `classify` (load-bearing); `SessionLimitPause` propagates through 5 guarded excepts to the worker. Kills the queue-vacuum (`hogs-1`). Claim-gate SQL untouched. 1121 offline + 14 real-PG green; 1 acceptance regression fixed + behavioral test added. Single head 0036. Routed to gatekeeper. |
| 0090 | 2026-06-26 | feat/prompt-optimization-port → Nggaev-v2 | **Prompt optimization port (Class-A Infra_prompts → `_general`, surgical).** Ported real optimizations from the external spec repo as targeted edits (NOT a replace — our prompts already distilled the same lineage; source is a different-pipeline spec set). **Fixed a confirmed bug:** flashcards deck size keyed off a dead `Mode: Easy/Hard` var (never substituted; `difficulty=None` since 0067) → grade-band sizing. Per-phase enrichments across all 11 phases + `_FC_`/`_CBP_`/`_LANG_UZBEK` blocks (common-mistake distractors, DPE rubric, per-pattern block counts, confidence-feedback steer, +pair types, boss counterfactual, Uzbek register/gloss). Dropped all contract-breakers (JSON/scoring/SVG/retrieve-from-address/per-family pinning); deferred new games (Listening/Video/Quizlet modes). New `check_prompt_render.py` no-LLM gate (55 combos) ran per task. No migration. Full suite **1122 passed**; real Vertex smoke shows optimizations live. Held un-merged pending gate + port-first campaign ordering. |
| 0091 | 2026-06-26 | extract-toc-api → Nggaev-v2 | **TOC extraction over Vertex API (no gemini-CLI OAuth).** Book-upload TOC extraction was a job-less gemini spawn → cli auth-scrub → CLI OAuth fallback (`initOauthClient`) that fails on a headless all-Vertex head, so text-PDF uploads couldn't get a TOC. Fix mirrors `summarize_lesson`'s local-text→api pattern: new `EXTRACT_TOC_TRANSPORT` setting (`cli` default \| `api`, validator rejects junk loudly), `toc_extractor.run` forwards it, and the text-usable branch ANDs `transport != "api"` into `keep_pdf` so api sends `attachments=[]` — **mandatory** because `api_transport.generate` raises `NotImplementedError` on any attachment; the local TOC text rides in as `lesson_context`. Vision/scanned stays cli unconditionally (SDK is text-only). `_auth_env` not bypassed. No migration. SDD; controller stress-tested each commit. **1133 passed / 0 failed**; real Vertex smoke on the failing book `860e86aa` → 64 entries, no `initOauthClient`, `auth_mode='api'`. Docs de-staled. Scanned-over-api out of scope. |
| 0092 | 2026-06-26 | launcher-persist-selections → Nggaev-v2 | **Persist launcher selections across navigation (launcher-persist-selections-1).** Fleet launcher's per-book card held all 10 selections as plain `useState` → navigating away discarded them, remount reset to defaults. FE-only fix: new pure `lib/launcher-config.ts` (per-`book_id` `localStorage` load/save, try/catch→`{}`/no-op, returns `Partial` for forward-compat); `ReadyCard` lazy-inits the 10 states from it + persists on change (never ephemeral UI state). **Load-bearing guards:** the apiSupported-reset + model-seeding effects treated unloaded-manifest as "unsupported" → would demote a restored `api`/model on cold load; added `if(!modelsQ.data)return` to both so they validate on loaded data (restore-then-validate, NOT a gate bypass — unservable picks still grey/reset once data lands; also fixes a latent default-`api`→`cli` cold-load collapse). No migration, no FE test runner → `tsc`+`vite build` clean; storage-layer behavioral test 6/6. In-browser behavioral acceptance (nav-away/return, hard-refresh, unservable-greys, two-books) is the gatekeeper's per agreement. |
| 0093 | 2026-06-26 | monitor-card-clarity → Nggaev-v2 | **Monitor card clarity: honest transport chip + per-transport split (monitor-card-clarity-1).** The monitor batch card's per-transport chip was binary `complete?` — but server `complete`="all jobs terminal" is also true when everything failed, so an all-failed transport (English-CLI 22 failed/0 done) showed green "complete". FE-only fix: new pure `lib/batch-status.ts` `transportRowStatus` → 4-way verdict (complete green only if all succeeded / failed red if 0 done / partial amber if mixed / in progress), driving a 4-variant chip; multi-transport header appends `done X CLI · Y API` (each transport's own count, **never summed** — overlap makes a sum >100%, proven on `62865c70`). Server flag + RollupBar untouched. No migration; `tsc`+`vite build` clean; pure-helper test 7/7. Item 2(b) accurate combined `X/31` (distinct-on-any-transport) needs a backend dedup rollup → deferred to R18. In-browser chip/header eyeball is the gatekeeper's. |
| 0094 | 2026-06-26 | api-vision-1 → Nggaev-v2 | **gemini vision over Vertex (scanned-book TOC + lesson-extract).** `api_transport._gemini` now accepts PDF/image attachments (multimodal `Part`s; claude stays text-only, guard relocated); `extract_toc` vision branch + `summarize_lesson_vision`/pipeline keep `transport="api"` for gemini (else cli) so scanned books OCR over Vertex instead of the OAuth-failing CLI. `auth_mode`/`error_message` follow the real transport. Cred path = `os.environ` Vertex SA (early return in `_spawn`). SDD + opus review CLEAN; **real Vertex multimodal smoke PASS** (1315 prompt tokens, PDF ingested). No migration; 1139 pass / 5 notion-503 env. Binding e020d51f re-extract = gatekeeper follow-up. |
