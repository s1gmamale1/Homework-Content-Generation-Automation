# Project Wishlist — capture inbox

> Raw capture of bugs / issues / ideas — **just the idea itself**, one line each.
> No analysis here. When an item is understood (root cause + deliverable), promote it
> to [ROADMAP.md](./ROADMAP.md) and remove it from this list.
> Tracked in git (committed alongside the work).

## Open

- `cli-failover-tests-red-1`: 2 PRE-EXISTING failures in `tests/services/test_failover_api.py` (`test_cli_failover_still_crosses_providers`, `test_api_failover_default_transport_is_cli`) — reproduced identically on pristine bases `211058f` AND `a99b676` in clean trees (proven at the #82 gate, 2026-07-06; not #82's). Symptom: the cli failover chain from claude only reaches `{gemini}`, never `{codex,kimi,opencode}` (`AssertionError: {'codex','kimi','opencode'} & {'gemini'}`, test_failover_api.py:51). Something drifted the cli chain (or its env-dependence) after the CQ-era suites ran green at 1350–1404. cli transport is retired operationally but kept-working by policy — root-cause the drift, fix code or test to the intended contract. Canonical bar is currently 1405 passed + these 2 red.
- `extract-gateb-short-lesson-fp-1`: Gate B's bare length floor (`extract_min_summary_chars=400`) false-positives on genuinely COMPACT lessons — Algebra-8 §5 (ko'paytirish/bo'lish, 3 pages, mostly exercises) legitimately summarizes to ~380-650 chars; worker runs landed 378/213 → "likely a refusal" fail ×2, while an identical in-process call produced a correct 643-char summary (2026-07-03, job 1e8fc0c2). Fix direction: refusal-SHAPE detection (lesson-term overlap / refusal-phrase check) instead of raw length, OR fold into the extract **coverage-contract** rework (structured enumerated extracts never dip under the floor). Note: cross-job extract cache missed because the extract prompt hash changed since June — cached 1,355-char §5 extract exists but is unreachable.

- `toc-reextract-fk-blocked-1`: TOC re-extraction is impossible for any book with jobs — the extractor's clear-before-insert DELETE on `toc_entries` hits `homework_jobs.toc_entry_id` (NOT NULL, no CASCADE) → `ForeignKeyViolationError`, book flips `failed`, old TOC survives (hit live 2026-07-06: G6 matematika `bbaa260a` chapter-level 6-row TOC couldn't re-extract past the HAJM job; operator workaround = backup packet md → NULL `agent_usages.homework_job_id` → delete job → retry). Also hit the sibling gap that dedup-by-sha returns an existing `toc_ready` book with zero feedback — FE toasts "Preparing" while nothing runs (`ingest_pdf` dedup path). Fix directions: re-point/park jobs on re-extract (match by title), or refuse with a LOUD 409 listing blocking jobs; plus a distinct FE state for dedup-hit ("book already exists — reusing").
- `notion-multipart-subject-clobber-1`: multi-part subjects («Matematika 1-qism»/«2-qism» — separate Notion pages, same `app_subject`) collapse in `notion_fetch.available_languages` (dict keyed by app_subject → LAST page wins), and the FE launcher prefers that map over the page the user actually clicked (`launcher.tsx` prepare: `subjectLangMap?.[lang]?.page_id ?? subjectPageId`) — so preparing part 1 silently downloads part 2's PDF, dedup returns the existing part-2 book, and no TOC extraction runs (hit live 2026-07-06, G6 matematika; unblocked by POSTing `/books/from-notion` with part 1's page id directly). Fix directions: key the language map by page_id (or app_subject+title), and/or make the FE trust the clicked page when it belongs to the selected language container.
- `batch-launch-lesson-filter-1`: batch launch fires a job per TOC row with no lesson/non-lesson discrimination — verified across the 10 UZ math books (2026-07-06 five-agent TOC sweep): only 282 of ~415 rows are real lessons; the rest are chapter headers («N-§» umbrellas in G8 geometry whose page ranges SWALLOW their child mavzu rows → near-duplicate packets), «Eslang» recalls, «O'ylab ko'ring» problem pages, TAKRORLASH revisions, tests/nazorat, biographies, Javoblar. Blind full-TOC batch ≈ +$80 waste + junk packets per 10 books; scales to the whole campaign. Fix directions: classify rows at TOC-extract time (LESSON/HEADER/RECALL/REVISION/TEST — the extractor prompt can tag them, or a deterministic title-pattern lint) + batch launcher defaults to LESSON-only with per-class opt-in toggles. Interim workaround: manual row picks per book (as done for G6/G8 waves).
- `homework-images-1`: **INTEGRATION, not a build** — an image-generation project already exists as a separate big project (user, 2026-07-03); the task is connecting it to this pipeline, not implementing from zero. First questions for whoever picks it up: where does it live (repo/API/service), what's its interface, and does it consume our described placeholders (`![visual: …](placeholder)`) as prompts — that's the natural seam. Then: cost/image, pipeline insertion point, Notion rendering (R9 ties in). (Meeting context: docs/meetings/2026-07-02-center6-homework-gen-extract.md #4.)

- `toc-reextract-override-1`: **re-extract TOC with a lever, only if verified-but-incomplete TOCs are observed in practice.** A plain "re-extract from `toc_ready`" escape hatch was designed then shelved 2026-07-01 as redundant with the vision validator (PR #74): the validator already routes major-incomplete/garbage TOCs to `toc_review` (retryable), and delete+re-upload already re-runs extraction. A same-model re-roll on the same PDF adds ~nothing. The version worth building is re-extract carrying an outcome-changing knob — a different extract **provider/model** and/or a **page-range hint** — as a deliberate operator tool for TOCs the validator waves through as `verified`/`skipped` but that are subtly incomplete. Don't build speculatively; wait for the campaign to show the case is real (validator is conservative: "major sections missing" only).
- ~~`notion-en-container-regex-1`: tighten the English grade-container regex before any consumer wires `available_languages`~~ — ✅ **SHIPPED (verified 2026-07-01 code audit).** Both halves of the premise are now false in code: the matcher is `_LANG_CONTAINER_RE["en"] = re.compile(r"-\s*(english|inglizcha|ingliz)\b", re.I)` (`notion_fetch.py:114` — bare `grade` **dropped**, with an inline comment citing the exact phantom-English-edition rationale), and `available_languages` now has a live caller (`GET` `get_available_languages` → `notion_fetch.available_languages`, `app/api/v1/notion.py:39-49`). The item's own gating trigger ("the first phase that surfaces `available_languages` MUST tighten this first") has been satisfied.

> Audited against current code **2026-06-04** (post job-resilience [0031] + lesson-matching [0032]). Items that referenced now-deleted code (structured schemas, source-map, source-fidelity) were removed as moot — see **Done / promoted**. Worked-up items live in [ROADMAP.md](./ROADMAP.md): **R9** (Notion SVG/tables→escaped text), **R10** (broken-font PDF → near-empty TOC), **R11** (provider failover not legibly recorded). Larger planned work (WS5 full Uzbek contract; Notion **Phase 2 pull**) tracked in `next-steps-flow-v2` memory. NOTE: a few inline `pipeline.py:NNN` line-refs below predate the resilience resume code growing `run()` — treat them as approximate.

> **Deep-system-review cluster — filed 2026-06-18** (each claim independently re-verified at tip `ccb88b4` before logging; rejected the review's "no `ondelete=CASCADE`" claim — CASCADE/SET NULL already present on toc_entry/phase_output/agent_usage). `judge-fidelity-1`, `judge-softfail-1`, and `judge-claimgate-1` **SHIPPED** (worklog [0079], cluster-3-judge-quality). The golden-eval harness (ROADMAP R20) is the follow-on: R20 now builds on the shipped fidelity check + `judge_status`.

- `concurrency-knob-1` (read-the-right-knob half SHIPPED [0077]; **Phase 1 reactive backoff SHIPPED 2026-06-25 (`90b714d`/`c064040`)**; **Phase 2 proactive token-bucket still OPEN**): the semaphore reads `agent_max_concurrency` (`gemini_max_concurrency` deprecated fallback). **Phase 1 (reactive) — DONE:** `agent._spawn` is now a thin wrapper that retries a transient 429 (`_is_rate_limited` → `429`/`RESOURCE_EXHAUSTED`/`rate_limit`/`overloaded_error`/`too many requests`; NOT auth 401/403 or `MAX_TOKENS`) with exponential backoff + jitter (`_rate_limit_delay`, env `RATE_LIMIT_MAX_RETRIES`/`RATE_LIMIT_BASE_DELAY_SECONDS`/`RATE_LIMIT_MAX_DELAY_SECONDS`); the backoff sleep holds no concurrency slot; `_spawn_once` is the unchanged single-attempt body. So a Vertex over-quota blip now throttles-and-retries instead of mass-failing. **Phase 2 (proactive) — STILL OPEN (needs Vertex quota numbers):** the cap is still a **module-global → per-process** `asyncio.Semaphore` (`agent.py`), so N worker processes = N×cap; the proactive fix is a **shared token-bucket per `(provider, model)`** (DB/Redis) so the fleet-combined concurrency is capped, not just per-process. **Confirmed live 2026-06-24** (gemini-API relaunch, 35 jobs across Mac+Oliver, content `gemini-2.5-pro` + judge `gemini-3.1-pro-preview`): both PCs share **one Vertex project**, so combined per-process concurrency (4×2) hit **429 `RESOURCE_EXHAUSTED`** repeatedly (Mac 39 / Oliver 18). Non-fatal — retried, **0 job failures** — but it **degraded the quality gate**: **27 phases shipped ungraded** (`judge_status="unavailable"`; the judge 429'd both tries → soft-fail, ties [[judge-refusal-1]]) + **4 `major_regen_failed`**. Preview/pro models carry the tightest Vertex quotas. **Interim mitigation:** lower `WORKER_CONCURRENCY` and/or move the judge off the `-preview` model to `gemini-2.5-pro`/`flash`.
- **✅ SHIPPED [0087]** (cluster-7) `judge-refusal-1` (residual after `judge-softfail-1` shipped in [0079]): **a claude judge content-policy "Usage Policy" refusal is not distinguished from a transient failure.** A cli refusal fails to parse a `Verdict` → degrades to `judge_status="unavailable"` (same bucket as a CLI/timeout error) and now also burns the [0079] **retry-once** on a refusal that will never self-heal. Observed live 2026-06-18 (algebra-g9, claude judge) — some phases shipped effectively ungraded. **Fix shape:** detect the refusal shape in `phase_judge` (distinct `judge_status="refused"`, skip the retry-once for it) so mass-gen can surface policy-declined-ungraded phases distinctly. Quality/observability; builds on the shipped `judge_status`.
- **✅ SHIPPED [0087]** (cluster-7, Option B — generator-aware `_self_fallback`, non-self for ANY generator; also fixed `worker._compute_capabilities`) `judge-self-fallback-1` **(surfaced 2026-06-25, gate of plan #1):** **`model_tiers._SELF_FALLBACK = ("gemini","gemini-3.1-pro-preview")` defeats the self-grade guard for a gemini-3.1-pro-preview GENERATOR.** `judge_model_for` returns `_SELF_FALLBACK` when judge==generator, but the constant IS gemini-3.1-pro-preview, so that generator gets "fallback"-judged by itself (the code docstring admits it). Became live-config-relevant when `.env` set `JUDGE_MODEL=gemini-3.1-pro-preview` → makes `tests/services/test_judge_resolution.py::{test_null_judge_model_same_provider_is_not_self_grade, test_self_grade_fallback_is_never_self_for_a_gemini_3_1_generator}` go RED (proven: both PASS on the default claude judge). **Campaign-safe** (generator is 2.5-pro/flash, never 3.1-pro-preview → no self-grade triggers). Two fixes: **(a) test-hygiene (small)** — those 2 tests read ambient `settings.judge` so they break on any non-default operator judge; monkeypatch `settings.judge` so they're config-independent; **(b) product** — retarget `_SELF_FALLBACK` to a non-gemini-3.1 frontier peer so the guard holds even when the judge default IS gemini-3.1-pro-preview (ties the "to generate with 3.1-pro-preview the judge must differ" finding).
- ~~`fleet-session-limit-autopause-1`~~ — **✅ SHIPPED 2026-06-26 (worklog [0089], cluster-5-fleet-resilience).** Ship-BOTH delivered: `failure_classifier.is_session_limit` (keyed on the `resets <clock-time>` clause) + `parse_session_limit_reset` (→ next-occurrence UTC); per-batch `session_limit_strategy` (mig 0036) + `SESSION_LIMIT_STRATEGY` env default + resolver + launcher control; `_run_with_failover` checks `is_session_limit` BEFORE `classify` → **pause** raises `SessionLimitPause(reset_at)` (worker requeues w/o burning an attempt + whole-worker self-cooldown until reset, auto-resume) / **switch** fails over off claude down the chain. Original **(user idea, campaign-critical — live grade-8 run 2026-06-23):** **on a Claude session-limit, auto-pause + auto-resume at the stated reset time instead of fail-fast.** The claude CLI returns `rc=1` with `result="You've hit your session limit · resets 12:50am (America/Chicago)"` — **the error states the exact reset time; exploit it.** Today the job is marked **failed** and the worker keeps claiming + insta-failing the rest of the queue ([[fleet-limited-worker-hogs-1]]). **Deliverable:** (1) classify the session-limit error distinctly in the agent/provider layer + **parse the reset timestamp** (distinct from the context-overflow `"Prompt is too long"` rc=1 and generic errors); (2) on hit, **don't terminally fail the job** — return it to `pending` (transient/retryable); (3) **stop claiming until reset** — either the limited worker self-drains until the parsed reset time (per-account: the limit is per claude login) OR pause the batch with a scheduled auto-resume; (4) **auto-resume at the reset time.** Open design decision — THREE strategic responses to a session-limit: (a) per-worker/per-account backoff (more correct — limit is per-login, lets a non-limited account keep working); (b) whole-batch pause+schedule (simpler, the user's framing); **(c) failover-the-role-to-a-non-limited model** (operator 2026-06-26: on session-limit, *switch* the affected role — e.g. claude-CLI extract → gemini — instead of waiting hours for the reset → keeps the batch MOVING with zero reset-latency; needs the session-limit classifier + a "degrade this role to a fallback model" path, since the general failover currently EXCLUDES claude and treats a session-limit as terminal). Surface (b) vs (c) as an operator choice: pause-and-wait vs switch-and-continue. Builds on C5/P1 pause primitive + C5/P2 backoff. The enabler for unattended Max-CLI mass-gen.
- ~~`fleet-limited-worker-hogs-1`~~ — **✅ SHIPPED 2026-06-26 (worklog [0089], cluster-5-fleet-resilience).** The vacuum is killed both ways: under **switch** the session-limited job no longer fails (completes on the fallback); under **pause** the limited worker **self-cooldowns** (whole-worker `_in_cooldown` gate skips `claim_next_job` until the parsed reset, auto-resumes) so it can't out-claim a healthy peer, and the requeued job is `scheduled_at=now()` (claimable now) so a healthy peer grabs it immediately. Original **(live grade-8 run 2026-06-23):** **a rate/session-limited worker fail-fast-vacuums the queue + starves healthy workers.** Once the Oliver claude-Max account hit its session limit, its jobs failed **sub-second at extract**, so Oliver out-claimed the slow but **un-limited Mac account** and insta-failed **33 jobs** — English got **0 done** because Oliver grabbed all 22 and failed them (Algebra: 18 done / 13 failed; English 0/22). **Fix:** a worker hitting a provider limit must **back off / stop claiming** (ties to [[fleet-session-limit-autopause-1]] + C5/P2 token-bucket + Retry-After backoff), not keep claiming. Prerequisite for the auto-pause feature. Mitigation today: **lower `WORKER_CONCURRENCY`** (4×2=8 concurrent claude streams burned the Max session window fast) + stagger accounts.
- ~~`extract-claude-context-1`~~ — **✅ CLOSED (2026-06-25 code audit): oversize extract ALREADY page-scopes (`pipeline.py:800` `read_page_range_text` on `extract_text_is_oversize`); combined with the opus-4-8 1M finding it's moot for these books.** Original **(live grade-8 run 2026-06-23):** **`EXTRACT_PROVIDER=claude` overflows claude's 200K context on big books.** Extract feeds whole-book text capped at `extract_max_text_chars=600_000` ≈ **~205K tokens** for dense Uzbek-math text → claude `result="Prompt is too long"`, `contextWindow:200000`, rc=1 → codex failover (intermittent, ~1 in 8 Algebra extracts). Extract was pinned to **gemini-flash (1M ctx)** for exactly this high-input/low-value summary. **Fix when staying claude-only:** lower `EXTRACT_MAX_TEXT_CHARS` to ~400000 (big books then page-scope to the lesson's pages → small input fits claude's 200K) — or keep extract on gemini. Small book (English 104pg) is under the limit and fine on claude. **Addendum 2026-06-24:** an opus-CLI extract smoke on the same Algebra book measured the whole-book text at only **~84K tokens** (337K chars) and `claude-opus-4-8` (1M ctx) handled it clean — so the original "~205K / overflows 200K" estimate was high, and opus-4-8 extract does NOT overflow this book; the risk is specific to **200K-ctx claude models** (sonnet/opus-4-7) on the largest books.
- `fleet-net-1` — **CODE HALF ✅ SHIPPED 2026-06-26 (worklog [0089], cluster-5-fleet-resilience):** `agent._spawn` now retries transient connection/DNS errors (`getaddrinfo`/`NameResolution`/`HTTPSConnectionPool`/`WinError 10053/121/1232`/`connection aborted`/`timeout`) with the existing 429 backoff, so a DNS/connection blip throttles-and-retries instead of dropping the call (auth/MAX_TOKENS/session-limit stay terminal). **OPS HALF STILL OPEN (operator):** put Oliver on **wired Ethernet** + a stable resolver, and **NTP-align both clocks** (ties `fleet-ops-2`) — the root-cause network/DNS unreliability is host-side and not fixable in app code. Original **(live gemini-API run 2026-06-24):** **Windows worker (Oliver) has intermittent LAN instability to the head.** Today's run logged `OSError [WinError 1232] "The network location cannot be reached"` + `[WinError 121] semaphore timeout` + `ConnectionAbortedError`, incl. one **job CRASH** (`Oliver:14984 job=f85f3b20 … CRASHED`, ~09:48). All recovered via orphan-reclaim (**0 permanent failures**) but it adds latency, log noise, and crash→re-claim churn. Likely Oliver↔head Wi-Fi / SMB / DB-connection flakiness. **Fix/mitigation:** put Oliver on wired Ethernet; check whether the reachability errors hit the PDF-pull (R13) path, the DB, or `FLEET_HEAD_URL` calls; add bounded retry around head-facing network calls. Also surfaced: the two PCs' **wall clocks differ** (Oliver log offset from Mac) → **NTP-align both** so cross-worker log correlation + duration analytics are trustworthy (ties [[fleet-ops-2]]). **Sharpened 2026-06-24 (log re-check):** the instability is **not just LAN-to-head — it hits Oliver's OUTBOUND calls to Google too.** 19 judge calls failed `HTTPSConnectionPool(host='oauth2.googleapis.com', port=443): Max retries exceeded`; the underlying cause is **DNS — `getaddrinfo failed`/`NameResolutionError` (×25+)** plus `WinError 10053` connection-aborts, with **ZERO `429`/`RESOURCE_EXHAUSTED` on those lines** → **verified genuine network/DNS failure, NOT rate-limiting.** Consequence: even after the token-bucket kills the 429s, Oliver would STILL drop API calls (it can't resolve/reach `oauth2.googleapis.com` to fetch the OAuth token) → these contributed to the run's 27 ungraded phases alongside the Vertex 429s. **Fix is network-side:** wired Ethernet + a stable resolver on Oliver (and the bounded-retry-around-API-calls would also soften transient DNS blips).
- `pg-hba-ipv6-1` **(live run 2026-06-24):** **Oliver's DB connections sometimes arrive over IPv6 link-local and are rejected.** `asyncpg.InvalidAuthorizationSpecificationError: no pg_hba.conf entry for host "fe80::…%en1", user "edu", database "edu_copy"` (×22) — the head's `pg_hba.conf` trust line is **IPv4-only** (`192.168.1.0/24`), so an IPv6-resolved connection matches no rule. Intermittent (asyncpg happy-eyeballs / hostname→AAAA). **Fix:** pin Oliver's `DATABASE_URL` to the literal IPv4 `192.168.1.15` (avoid hostname→IPv6), or add an IPv6 `host` line to `pg_hba.conf`. Latent infra gap in the dev-only LAN-Postgres posture (see [[fleet-infra-1]]).
- ~~`log-hygiene-1`~~ — **✅ SHIPPED 2026-06-25 (`b10f12c`).** `app/log.py` format now carries the date (`{time:YYYY-MM-DD HH:mm:ss.SSS}`) and the `var/server.log` file sink rotates **daily** at midnight (`rotation="00:00"`, retention still 7 days) instead of by 20 MB size, so a run is greppable in isolation rather than conflated across the midnight boundary. Original **(surfaced doing the 2026-06-24 log verification):** **`var/server.log` is dateless and not per-run-rotated → consecutive runs conflate, analysis is hard.** The loguru format emitted `HH:MM:SS.mmm` with no date and rotated only at 20 MB, so yesterday's claude run and today's gemini run interleaved in one file with a silent midnight wrap; raw error counts (e.g. "136 session limit", "48 extract fails") were mostly the *prior* run and had to be model-discriminated + DB-cross-checked to attribute correctly.
- ~~`api-error-capture-1`~~ — **✅ SHIPPED 2026-06-25 (`a84f4c9`).** New pure `agent._spawn_failure_message(provider, transport, rc, stderr, text)` builds a transport-aware `error_message` (says "api" not "CLI" on the SDK path + embeds the real error preview), wired into the `run_phase` rc≠0 branch and the `summarize_lesson` failure path; both also write the raw error into `agent_usages.raw_envelope` via `extra_envelope["error"] = stderr[:2000]`, so `/agent/stats` + cost/quality reports can categorize a 429/DNS/auth failure from the DB alone. Original **(surfaced 2026-06-24):** a failed `transport=api` call recorded `error_message="gemini CLI exited rc=1"` (CLI wording, wrong for the SDK path) and `raw_envelope` held only `{"attempt":1,"phase_name":…}` — the actual `429 RESOURCE_EXHAUSTED`/Vertex error only landed in the worker's `server.log` (why the 2026-06-24 verification needed log-grep, not just the DB).
- ~~`launcher-capability-gate-1`~~ — **✅ SHIPPED 2026-06-26 (worklog [0085], launcher-capability-gate, Cluster 5 slice 1).** Workers publish a `{cli,api}` capability blob on heartbeat (`workers.capabilities` JSONB, migration 0035; `CAPABILITY_BLOB` shares `_api_capable` with claim-gate `CAPABILITIES`); head unions over ONLINE workers (`aggregate_fleet_capability`, fail-open at zero → offline banner); `/agent/models` gains a `fleet` block; FE `serveability` helpers grey out unservable `(provider × transport)` picks (label-suffix reason, since a disabled Radix item's `title` won't fire) + 5s refetch + reset guards; per-role `inherit` resolves to the job transport. Granularity is **provider×transport** (per-model/quota out of scope). Claim gate untouched. Original **(surfaced 2026-06-25, operator):** **the launcher offers provider/model/transport combos the fleet can't actually serve → jobs launch and sit `pending` forever.** `/agent/models` returns the full STATIC `MODEL_MANIFEST` (every provider + every model) + an `api_supported` map; the FE (`launcher.tsx`, `RoleAgentControls.tsx:51-52`) lists ALL manifest providers/models and only gates the **api-transport toggle** by `api_supported` (claude/gemini) — it does NOT reflect **live fleet capability**: which provider CLIs are installed on the workers, and which credentials are present (no `ANTHROPIC_API_KEY` → claude-API jobs unclaimable; kimi/codex/opencode shown even when not installed; preview gemini models with no quota shown like GA). The worker `CAPABILITIES` (`worker._compute_capabilities`: `can_claude_api`/`can_gemini_api` + `provider_cli_installed`) is computed per-worker but **never surfaced to the FE**. **Deliverable:** a backend endpoint exposing aggregate fleet capability (union over LIVE workers of installed provider-CLIs + per-provider api creds), and FE filtering/greying of unservable `(provider, model, transport)` picks with a reason tooltip. Prevents the phantom-pending-job footgun (the api-job-claim-gate class, [[per-job-selection-overrides-env]]). Campaign-relevant: mixed credentials across machines make this trivially easy to trip.
- `sse-multipod-1`: **SSE live progress silently breaks when workers run as separate pods.** `events_bus._subscribers` is an in-process `dict` (`events_bus.py:5`); a standalone worker publishes to ITS process, so a browser connected to the API pod only gets the initial DB replay, then a frozen stream. Fine today (embedded worker = same process) but a fleet-scale (`WORKER_CONCURRENCY=0` API pods + dedicated worker pods) regression. **Fix:** back the bus with Postgres `LISTEN/NOTIFY` (or Redis pub/sub). Verified 2026-06-18.
- `json-preamble-1`: **Claude (CLI) wraps schema JSON in a conversational preamble → attempt-1 validation fail + wasted retry.** When a phase requests JSON (TOC `ExtractedTOC`, judge `Verdict`, any `schema=` caller), claude via the CLI sometimes prepends prose ("Found the MUNDARIJA…", "Looking at the output…") before the JSON. The parse does `_strip_code_fences(text).strip()` (strips ```fences``` + whitespace) but NOT a prose preamble, so `model_validate_json` fails at line 1 col 1 (`agent.py:793-795` `run_phase`, `agent.py:1330` `extract_toc`). It self-heals via the **2-attempt retry** (re-prompt → attempt 2 clean), so the final output is correct — but the wasted spawn is **expensive for scanned-TOC vision** (a full ~8.5-min re-extract + claude quota burned just because attempt 1 had a preamble) and adds latency to the judge. Claude-CLI-specific (agentic narration); gemini / the structured SDK rarely do it. **Fix shape:** before `model_validate_json`, extract the outermost JSON object from a prose-wrapped response (balanced-brace scan from the first `{`, or a `{…}` capture) — pure pre-parse step, clean JSON unaffected; apply at both parse sites (`run_phase` + `extract_toc`). Small **gated** plan (touches the shared parse path). **Confirmed live 2026-06-18** on TOC (`a0173601`) AND a generation judge run (`__judge__`, claude-opus). User decision: log-only for now.
- ~~`fetch-1`: **Books >50MB (and scanned/text-massive books) still unfetchable**~~ **✅ SHIPPED 2026-06-27 (worklog [0095]).** The >50 MB wall was the only blocker — and it was never an LLM limit (Explore audit: every downstream read is bounded). Fix: `max_file_mb` default 50→250 (ingest/RAM guard; 250 not 600 to keep per-upload RAM bounded) + de-conflated the upload-413/notion-422 reject messages (name `MAX_FILE_MB`, drop the stale "shrink and upload manually"). Shrink rejected (only `pypdf` installed; image-downsample is lossy for scanned pages the vision extractor reads). Real smoke `scripts/smoke_fetch1_giant.py` (54.4 MB synthetic: accepted, all reads bounded, peak RSS 752 MB no OOM). **Follow-ups logged below:** `ingest-stream-1` + `extract-perf-1`. Operator: live head `.env` pins `MAX_FILE_MB=50` → raise to 250 before ingesting the 67.5 MB book. **Original (history):**
- `ingest-stream-1`: **Stream upload + notion-fetch to disk with incremental sha256** — today both hold the whole body in RAM, then hash, then `write_bytes` (`books.py:71-105`, `notion_fetch.py:155`). Fine for the campaign's ≤250 MB textbooks, but a 300 MB+ book × concurrent uploads is a latent OOM — and it's the reason the `fetch-1` cap is held at 250, not higher. **Fix:** stream the GET/UploadFile chunk-by-chunk to a temp file, `hashlib.sha256.update()` per chunk, then dedup-check + promote (touches the dedup-by-hash flow — `find_ready_by_hash`). **Trigger only if** genuinely huge books must be ingested. Logged 2026-06-27 (fetch-1 [0095] follow-up).
- `extract-perf-1`: **`read_whole_book_text` is slow on large scanned books** — measured ~106 s for a 352-page scanned PDF before the pipeline detects sparse text (`extract_text_is_too_sparse`) and falls to the bounded vision window (`pipeline.py:881` → 907). Every lesson-extract on such a book pays this whole-book text scan up front for ~30 K chars of near-garbage. Pre-existing (not introduced by fetch-1, which only raised the cap). **Fix shape:** sample density on the first N pages (or read page count + a few pages) to short-circuit to vision before the full-book `extract_text` loop. Logged 2026-06-27 (fetch-1 [0095] observation).
- `fetch-1-history`: **(superseded — kept for the staleness trail)** REWRITTEN 2026-06-11 (staleness audit): the old headline ("20MB fetch ceiling hits ~43% of textbooks") is STALE — the fetch path now rejects at `settings.max_file_mb` = **50MB** (`notion_fetch.py:116`; the separate 20MB `_TOC_MAX_BYTES` reject no longer exists), matching uploads, so most formerly-blocked image-heavy books fetch today. `extract_toc` still drops the gemini attachment >20MB (`_GEMINI_PDF_MAX_BYTES`, `agent.py:110`) and falls back to local pypdf text — graceful, not a reject. **Remaining real walls:** (a) **>50MB giants** (e.g. the 497MB grade-9 Jahon tarixi) → need shrink/subset-TOC pre-fetch; ~~(b) per-lesson whole-text gate → text-massive books need subset extraction~~ **SHIPPED 2026-06-17 (worklog 0070):** oversize lesson-extract now scopes to the lesson's pages as text (`read_page_range_text` + subset → `summarize_lesson`, keeps api); ~~(c) scanned/no-text-layer books → need OCR~~ **lesson-extract half SHIPPED 2026-06-17 (worklog 0070):** scanned/sparse lesson bodies now vision-attach a page-window (`summarize_lesson_vision`, forced cli) + per-page density gate (`extract_text_is_too_sparse`); ~~scanned-TOC half~~ **SHIPPED (staleness audit 2026-06-18):** `extract_toc` (`agent.py:1195-1217`) now detects sparse/scanned text and **vision-attaches a bounded front+back page window** (`toc_mode="vision_toc"`, forced cli) instead of failing at upload. **Only remaining wall: the >50MB giants** (item (a) above). **Unverified empirical claim:** that a real 20–50MB book's text fits under 600K — offered to test (fetch one, measure) but not yet run. **DEFERRED 2026-06-26 (Cluster 8 scope decision):** the >50MB-giants auto-shrink is held to the operator escape hatch (the fetch reject already says "shrink and upload manually") until the definitive campaign subject list proves a *required* textbook is only available as a >50MB file.
- ~~`r13-fetch-1`: **De-dupe concurrent same-book PDF fetches in one worker**~~ **✅ SHIPPED 2026-06-26 (worklog [0088], Cluster 8).** Per-`book_id` `threading.Lock` in `ensure_book_pdf_sync` (module dict + master-lock guard): lock-free fast path → double-checked `_cached_ok` re-check → fetch, so the first thread downloads and the rest wait then hit the cache (RED-proved 5 downloads → 1). The uuid4-distinct-temp collision fix (PR #9) remains as belt-and-suspenders. Per-process by design (cross-PC dedup is a non-goal).
- ~~`fetch-2`: Fetch From Notion takes the **first PDF in page order** when a subject page has multiple attachments — prefer the textbook (`darslik`) over the workbook (`ish daftari`).~~ **✅ SHIPPED 2026-06-26 (worklog [0088], Cluster 8).** `_first_pdf_block` now ranks `textbook(0) ▸ neutral(1) ▸ workbook(2)` (`_pdf_rank`/`_pdf_name`, `min((rank,page_order))`) — preference, not exclusion (workbook-only page still ingests). Was higher-impact than its old "low priority" tag: under mass-gen a workbook-first page silently became the whole subject batch's textbook. **Known limitation:** name-based ranking would rank a `*darslik javoblari.pdf` (answer-key) as a textbook — rare on subject pages; revisit if it bites.
- `cancel-1`: Cancel generation (worklog [0038]) follow-ons — (a) a **single full end-to-end live run** (cancel a real multi-phase generation via the HTTP endpoint, assert all parallel CLIs die + job `cancelled` + done phases preserved) + the **queue-cancel** and **resume-on-/retry** live paths (mechanism is proven link-by-link + a real-CLI tree-kill smoke, but not yet one full chain run); (b) a **sub-`heartbeat_seconds` cross-pod cancel watcher** — separate-pod cancel latency is bounded by `heartbeat_seconds` today (embedded is instant). Low urgency.
- `extract-1`: **TOC extraction — broken-font `/Gxx` glyph-loss case still poisons the TOC** (verified 2026-06-05, code-checked). Scope is NARROW: the heading-garble case (case-scramble, letters intact) is **already FIXED** (worklog [0036], `1fde640` — de-garble clause live at `extract_toc:1079-1085`). Only the **`/Gxx` glyph-loss** case is open: PDFs with no ToUnicode map → `_decode_glyph_text` (`agent.py:841`) substitutes glyph codes to cp1252 bytes; when the code≠byte it yields **real-looking WRONG letters**, and `has_local_toc_text=bool(text)` (`:1060`) is a non-empty check, not a readability one → garbage injected as authoritative TOC → near-empty/nonsense lesson list (e.g. Geometriya 2019). **NOT a clean drop-in fix** (assessed, deliberately not done): (a) **no false-positive-proof detector** — after decode the garbage looks like letters, and the prose Gate A (`validate_extract_text`, printable-ratio 0.55) would FALSE-POSITIVE on normal page-number/dot-leader-heavy TOC text → risks breaking *every* upload; (b) the obvious remediation "drop local text → fall through to gemini native PDF read" is the SAME path the gitignore block defeats, so detection may only turn garbage→empty, not garbage→correct. Needs a short brainstorm on TWO decisions: **how to detect glyph-loss without false-positiving normal TOCs**, and **loud-fail vs try-anyway**. LOW URGENCY — rare, and self-evident when it hits (near-empty lesson list). Ties to R10's open glyph-ID manifestation + the "scanned / image-only PDFs → needs OCR/vision" WISHLIST item (same no-usable-text-layer class — dedupe when picked up).
- `fe-redesign`: **Operator console (`web/`) full redesign — DEFERRED mid-brainstorm 2026-06-05.** Driver: looks dated/generic + clunky + missing things + unfinished. Agreed **3-slice sequencing** (each ships working, own spec→plan cycle): **Slice 1 Foundation** = delete dead student-play code (`components/boss-fight, games/, flashcards, memory-sprint, reading` — none routed) + establish design language + clean component kit; **Slice 2 IA/workflow** = restructure nav + upload→generate→monitor→review flow, job/queue visibility; **Slice 3 capabilities** = batch generation, live queue dashboard, Notion archive/validation status. **Start with Slice 1.** Aesthetic direction explored & user said "sounds good": **"Quiet Precision"** — editorial-clean, content-first, real material depth; *Newsreader* serif (display) + *Hanken Grotesk* (UI) + *JetBrains Mono* (metadata); restrained bronze/amber accent (keeps brand); the **live-generation hero** (9-phase pipeline progress) as the centerpiece for the "lose track of what's running" pain. Light-vs-dark NOT yet chosen. **Crafted mockup preserved → `docs/design/2026-06-05-console-redesign-quiet-precision.html`** (open in a browser — light & dark, same Library screen). Resume = re-enter brainstorming on Slice 1 from this mockup.

> **Browser QA sweep — filed 2026-06-29** (read-only exploratory pass over all 11 SPA routes via Playwright at tip `0c1881c`; auth + every page render clean, 0 console/JS/network errors; findings verified against FE+BE code). The two `launch-defaults-*` items are real bugs in the #53/#54 global-defaults feature; the rest are IA/UX.

- ~~`launcher-role-transport-default-1`: **Fleet launcher silently overrides the global Extract/Judge transport default.** Role transports init to a concrete `"cli"` (`launcher.tsx:581-582`) and are sent verbatim (`:749-750`); backend `resolve_role_transport_default` only honors the Settings default when the value is `"inherit"` (`agent_models.py:116`, `jobs.py:246`) → a concrete `"cli"` wins, so Settings → Extract/Judge=API is ignored on every fresh Fleet batch launch. Provider/model correctly default to Auto/`null` (they DO inherit). The Section launcher gets this right (`"inherit"`, `section.tsx:47-48`). Campaign impact: silent cli extract/judge on an all-Vertex head hits the OAuth-failing CLI path. **Fix:** default `:581-582` to `"inherit"`. Ties [[per-job-selection-overrides-env]].~~ **✅ SHIPPED 2026-06-29 (PR #56, worklog [0100])**
- ~~`single-job-launch-defaults-1`: Single-section `/generate` never applies the global launch-defaults~~ — ✅ **SHIPPED for role/transport/language (verified 2026-07-01 code audit); narrow content-tier residual re-filed as `single-job-content-defaults-1` below.** The single-section handler now fetches `ld = await launch_defaults_repo.get(session)` (`jobs.py:206`) and resolves judge/extract provider+model (`resolve_role_selection`, `:252-255`), judge/extract transport (`resolve_role_transport_default`, `:256-257`), and `output_language` (`:208`) against the global defaults — stamped at `:284-292`. The original "per-lesson launch ignores Settings" claim is stale.
- `single-job-content-defaults-1` (residual of `single-job-launch-defaults-1`): the **content-generator** provider/model/transport are still stored raw from the request body (`jobs.py:281-283`); the `content_provider`/`content_model`/`content_transport` launch-defaults columns (`app/models/launch_defaults.py:32-34`, surfaced in `settings.py:23-25`) are consumed by **neither** launch path (single `/generate` nor batch). So content-tier global defaults remain unwired — a per-lesson launch that omits content provider/model falls back to whatever the handler defaults to, not the Settings value. Small: apply the same `resolve_role_selection`/`resolve_role_transport_default` pattern to the content tier in both paths (or drop the unused columns if the tier is intentionally request-only).
- `download-url-tokenless-1`: **`GET /jobs/{id}` returns a token-less `download_url` field** (`jobs.py:444`) that 401s if trusted; the FE correctly ignores it and rebuilds the URL with the token (`api.jobDownloadUrl`), but any other API consumer would fail. Drop the field or document it. (Download endpoint itself verified correct: 200 zip of 11 per-phase md; 401+no-leak without token.)
- `launch-config-surfaces-1`: **Launch config is configured in 3 places** (Settings global defaults, Fleet batch launcher, Section per-lesson launcher) with the same provider/model/transport/extract/judge controls; high control density. Layered-by-design (launchers show `Auto → <default>` inline), but consider tucking extract/judge/TOC behind an "Advanced" disclosure now that globals exist. *(partially addressed by PR #56 — Content added to Settings, Judge/Extract removed from Fleet launcher; remaining: Advanced-disclosure / Section launcher still shows them)*
- `notion-upload-entrypoints-1`: **Notion-fetch / upload is reachable from ~4 places** — `Upload` nav tab, the Upload page's two CTAs, Library "+ Upload book", and Fleet "Prepare a subject → pull from Notion." Fleet's "Prepare a subject" duplicates Upload's "Fetch From Notion." Consolidate.
- `fleet-monitor-progress-overlap-1`: **Fleet renders inline batch progress** (LAUNCHED tray + % bars) while also telling the user to "Watch progress in Monitor" (the fuller dashboard). Consider linking the Fleet tray into Monitor instead of re-rendering progress.
- `section-filter-substring-1`: **Section/book filter is substring, not word-aware** — searching `burchak` (angle) also returns `uchburchak` (triangle) lessons. Low-impact relevance quirk.
- `settings-toc-model-cell-1`: **Settings → TOC row shows a disabled `—` Model cell** (TOC follows the extract provider, has no own model) — reads as missing/broken. Add a hint or hide the cell. _(PARTIAL, 2026-07-01 audit: the **provider** cell now carries a `"(via Extract)"` hint at `settings.tsx:348`, but the **Model** cell is still a bare muted `—` at `:350-352` — the exact ask is unshipped.)_

### Fleet — autonomous generation fleet (deferred from Phase 3 brainstorm 2026-06-08)

> Phases 0 / 1 / 0.5 / 2 / 3 / 4 SHIPPED (worklogs [0047]–[0051] + [0053], `feat/autonomous-fleet-engine` worktree). **Phase 4** = the real-time **CLI | API transport toggle** (claude+gemini api, per-job/per-batch `transport`, `auth_mode` attribution + per-transport `$`) — see [0053]; billed live smokes are operator-run (`docs/runbooks/phase4-transport-operator-acceptance.md`). Everything below was surfaced in the brainstorm and deferred; the `fleet-api-*` items are the post-Phase-4 deferrals (spec §8).

**Batch / fleet controls (deferred from Phase 3):**
- ~~`fleet-ctrl-3`~~: **MANUAL pause/resume SHIPPED 2026-06-19 (Cluster 5 / P1, [0081])** — `POST /jobs/batch/{id}/pause|unpause` (reason "manual") + FE. (The claim-gate skip + auto budget-pause primitive shipped in C4/[0080].)
- ~~`fleet-ctrl-4`~~: **SHIPPED 2026-06-19 (Cluster 5 / P1, [0081])** — `POST /workers/{pc_id}/drain`, worker self-drains on its registry beat.
- ~~`fleet-cancelresume-ux-1`~~ — **✅ CLOSED (2026-06-25 code audit): IMPLEMENTED — toasts show counts (`launcher.tsx:719,732`) + batch cards show queue depth via `RollupBar`.** Original: **Cancel/Resume actions don't narrate their numbers → correct behavior reads as random/buggy.** The cluster-2 cancel/resume logic ([0078]) is correct, but the FE shows no counts: Resume sets ALL resumable lessons to `pending` yet only `WORKER_CONCURRENCY` (e.g. 4) run at once, so it *looks* like it "resumed 4 random jobs"; "Cancel all" silently cancels queued jobs too (pending + running = halt the batch), so it *looks* like it did more than asked. Pure display/copy, no behavior/DB change. **Fix shape:** (a) Resume toast → "Resumed N lessons — running X, queued N−X"; (b) "Cancel all" confirm/toast → "Cancel all N (X running + Y queued)?"; (c) show **queue depth** on the batch card ("X running · Y queued") so the N-lane queue is self-evident. Surfaced live 2026-06-18 (user confused why Resume started 4 + Cancel-all stopped the whole batch).

**Dashboard UX (beyond the Phase 3 view):**
- `fleet-ui-2`: **Live SSE dashboard** — push batch-funnel + PC-card updates instead of polling `GET /jobs/batches` + `/workers`.
- `fleet-ui-3`: **Past / historical batches view** + filter / search (Phase 3 MVP focuses on active batches). _(PARTIAL, 2026-07-01 audit: Monitor is no longer active-only — status filters incl. `complete`/`all` (`lib/monitor-filters.ts:6`) + language tabs + grade filter (`monitor.tsx:36,55,84`) make completed batches viewable/filterable. Still missing: a **free-text search** input and a dedicated history view.)_
- `fleet-ui-4`: Richer **PC cards** — current job, throughput, runtime, attempts (beyond online / idle / busy).
- ~~`fleet-ui-5` (restructure half)~~: **SHIPPED 2026-06-12** (worklog [0057], PR #4) — batches + workers moved to a new `/monitor` page; `/fleet` keeps launch + an `OnlineStrip` liveness line (amber when 0 workers). Built as chunk-3 sub-project 1. ~~Sub-project 2 — the sigma-designs visual reshape~~ — **✅ SHIPPED 2026-06-15 (PR #6, worklog [0059]); verified 2026-06-25 audit — this line was STALE.** Living-glass + AI rim-glow as shared foundation primitives: `globals.css` (`@property --ang`, aurora-drift/rim-spin/seg-pulse keyframes, `.glow-rim`/`.aurora-bloom`/`.seg-now`, Sigma `@theme` tokens), `lib/ui.ts` glass-token global re-skin (6 routes + 4 fleet comps), drifting-aurora `SpaceBackdrop`, OnlineStrip glow, monitor `.glow-rim`/`.seg-now`. **The current `SpaceBackdrop` + `lib/ui.ts` kit IS the sigma output** (a deep-verify agent misread it as "still space-theme"; the worklog/PR-files settled it) — see [[fe-design-system-pointers]].

**Generation accounts / cost (spec Phase 4):**

> The **real-time CLI | API transport toggle SHIPPED** (worklog [0053], 7 tasks) — per-job/per-batch `transport` enum (claude+gemini api), `_auth_env` adapter, `has_api_keys` claim gate, `auth_mode` attribution + per-transport `$` readout. The items below stay deferred (spec §8). The old `fleet-api-1` ("Gemini-API provider + toggle") is now MOOT (delivered by the toggle); `fleet-api-1` is reused for the deferred **batch-discount** transport.

- `fleet-api-1`: **Batch transport** (Gemini/Anthropic Batch REST, ~50% off, async submit/poll, per-wave batching across lessons) — slots in as a third `transport` enum value.
- `fleet-api-2`: **Credential rotation pool** — rotate multiple API keys per provider across the fleet (Phase 4 ships single key per provider).
- ~~`fleet-api-3`~~: **SHIPPED 2026-06-18 (Cluster 4, worklog [0080]).** Cost ledger (`app/repositories/cost.py`: per-batch / fleet-day / per-section) + budget monitor (`worker._budget_monitor`) + the pause-claim kill-switch: both caps (`COST_CAP_BATCH_USD` + `COST_CAP_FLEET_DAILY_USD`, 0=disabled) trip the batch-pause primitive (`batches.paused_*`) / fleet `budget_state` gate, AND-composed into `claim_next_job`. Reason-scoped (`batch-cap`/`fleet-daily-cap`); pause-claim (in-flight finishes, never hard-cancel). The batch-pause primitive is reused by C5's `fleet-ctrl-3`.
- ~~`fleet-api-4`~~: **SHIPPED 2026-06-18 (Cluster 4, worklog [0080]).** A force re-run over an already-api-completed section surfaces `prior_api_cost_usd` + `would_rebill` (detect+warn, force still proceeds; free resume/done-phase reuse unaffected). Typed reconciliation with C2's relaunch-confirm shape left as a follow-up.
- `fleet-api-5`: **codex API mode** — `CODEX_API_KEY` env (not `OPENAI_API_KEY`, which codex ignores) flips auth, and default models diverge between auth modes (gpt-5.5 subscription vs gpt-5.4 API). Deferred from Phase 4 (user: only Anthropic + Gemini api). The explicit-model rule already protects the divergence class; re-confirm the 401/key behavior against the installed codex version on pickup.
- ~~`fleet-api-6`~~: **SHIPPED 2026-06-11** — gemini api transport now accepts a Vertex AI service account (`GOOGLE_APPLICATION_CREDENTIALS` + `GOOGLE_CLOUD_PROJECT`, location defaults `global`) as a fallback when `GEMINI_API_KEY` is absent; claim gate accepts either. Verified by live smoke (auth_mode=api row, Vertex-billed). Explicit AI-Studio key still wins when both present; persisted `selectedType` must be removed on the worker (existing §3 rule).
- ~~`pricing-1`~~: **SHIPPED 2026-06-18 (Cluster 4, worklog [0080]).** Added `agent_usages.cache_creation_tokens` (migration `0030`, additive + backfill from `raw_envelope`); claude cache-write captured (api + cli envelopes) and priced at **1.25× input** in `cost_usd`. Per-provider cached-token semantics (gemini `prompt-includes-cached` vs claude disjoint) left intact — a regression test pins both branches (no-collapse rule).
- `fleet-ops-1`: **pin gemini-cli versions across the fleet** — workers currently run mixed versions (0.45.2 PC vs 0.46.0 Mac); behavior verified identical for everything Phase 4 depends on today, but unpinned CLI versions are a latent ops wobble (auth resolution + envelope shape are version-sensitive).
- `fleet-ops-2`: **make all completion timestamps DB-clock (finish the 0049 discipline)** — `c24f873` converted the 4 direct-UPDATE terminal paths (`jobs.py` mark_failed/cancel/reclaim) to `func.now()`, but the pipeline's happy-path/failure completions still write host-clock `_utcnow()` via `set_status(..., completed_at=…/started_at=…)` (`pipeline.py:119,239,268,351,618,654,734,815`) and `agent.py:480` (`agent_usages`). Harmless today (claim/reclaim WHERE logic is pure DB-clock; completed_at/started_at are display/analytics only; local DB ⇒ host==DB clock). Becomes real under multi-PC skew for cross-row/duration analytics (the benchmark's reason for existing). Fix = thread `func.now()` through `set_status`/`phase_repo.set_status` (assigning a SQL expr to the ORM attr) or have those repos stamp server-side. Touches set_status signature + many call sites → not a small fix.
- ~~`fleet-api-7`~~: **SHIPPED 2026-06-11** (worklog 0054, Phase 4.1) — per-job/per-batch `extract_transport`/`judge_transport` (`cli|api|inherit`, default `inherit`) + claim gate v2 (per-role capability routing incl. §4a judge self-fallback) + typed `AuthEnvError` + FE Extract/Judge billing selects; mixed-billing attribution proven by live smoke (gemini row `auth_mode=api` via Vertex + claude row `auth_mode=cli` in one run).

**Infra / ops (spec §8 / §9.1):**
- `fleet-infra-1`: **PgBouncer + hardened head** — the Phase-0 published-Postgres-on-LAN is dev-only; a production head needs connection pooling + lockdown.
- `fleet-infra-2`: **Worker discovery file / movable head** (≤60s reconnect) — workers find the head via a discovery file, not just a static `DATABASE_URL`.
- ~~`fleet-restart-reclaim-1`~~: **SHIPPED 2026-06-19 (Cluster 5 / P1, worklog [0081]).** Peer-aware `reclaim_orphans_on_startup`: reset-all (stale=0) only when no live peer is registered in the `workers` table; otherwise use the lease window (`reclaim_stale_seconds`) so a heartbeated peer job is never yanked on head restart.

**Validation gaps (untested as of the 2026-06-09 single-PC live run — engine proven end-to-end on ONE box: launch→queue→claim→9-phase pipeline→`done` with real content, live cancel/kill_tree reaped 0 orphans). These are what that run did NOT cover:**
- `fleet-test-1`: **Multi-PC contention with REAL generation** — only ever smoke-tested in Phase 0 (2 containers, no generation). The fleet's whole premise. **Gated on [[ROADMAP]] R13** (PDFs must reach every worker first).
- `fleet-test-2`: **Output quality + the LLM judge ([0037]).** ✅ **JUDGE CONFIRMED working + correct** via the 2026-06-09 run log: judge ran on every content phase (claude, `schema=Verdict`); severity gate fired correctly (regen ONCE on MAJOR only) — **10 major regens** across 7 lessons with legitimate catches (answer-leaking hints, concept named in the CBP setup, and a real **factual chronology error** — Ganza league dated 1260 vs textbook's mid-14thC, cross-checked against source + the output's own Learning Block). **Residual:** eyeball a *final* (post-regen) deliverable to judge polish; confirm regen output is re-accepted.
- `fleet-test-3`: **Live failure + failover in a batch** — STILL OPEN. The 2026-06-09 run had **0 failures / 0 failover events** (all on claude default, clean), so retry/backoff + provider-failover ([[R11]]/[0031]) never fired. Needs a *forced* provider failure (non-claude erroring, or a bad model string) to exercise the fall-through.
- `fleet-test-5`: **Notion Prepare end-to-end** (download→ingest→TOC) + **Notion archive** (write finished packet back) — grades loaded (200) but no new book was pulled, and archive was untouched.
- `fleet-test-6`: **Big-PDF (>20 MB) extract** (e.g. math-algebra 27 MB) — **CODE PREMISE CLOSED (verified 2026-07-01), live run still owed.** The "would fail at extract" premise is stale in code: `max_file_mb` default is 250 (`config.py:38`), gemini attachments >20 MB fall back to local text (`agent.py:130` `_GEMINI_PDF_MAX_BYTES`), oversize lesson-extract page-scopes via `read_page_range_text` (`agent.py:1202`, wired `pipeline.py:900-914`), and scanned-TOC vision shipped. A >20 MB book no longer fails at extract — but the end-to-end verification on a real 27 MB book was never run. Remaining scope is **operational** (run it), not code.
- `fleet-test-7`: **Multiple concurrent batches** — only one batch at a time was exercised.

### Effort B — faithful Infra-spec prompt rewrite — ✅ SHIPPED (worklog [0030], 2026-06-04)

> Done: **Option A** `{{FAMILY_RULES}}` token (family-of-4, not subject-of-7); CBP + Flashcards rewritten family-aware (**humanities CBP authored** — no source spec); light/compact/polish passes on the other 9 phases; all folded-in fixes (memory-check **≥2-of-3**, flashcards **in-prompt enum** since `FlashcardType` is deleted, reflection **`#` title**). Live-verified on real gemini history output (validator-clean, 0 dead-vocab). Full detail in worklog [0030]; spec `0dbb34a`, plan `61dc6c6`.
>
> **Still pending (the follow-on, NOT shipped):** the **WS5 Uzbek language contract**. ~~per-phase `phase_validator.RULES`~~ is **SUPERSEDED** — the LLM-phase-validator design (`docs/superpowers/specs/2026-06-04-llm-phase-validator-design.md`) *retires the deterministic rule engine entirely* (its plan deletes `phase_validator.py`) in favour of a self-verifying LLM judge. Do NOT hand-author `RULES`; that direction is dead.

### Backend

**API / events / dead code**
- `deadcode-1`: **`source_map_*` plumbing is dead but still threaded everywhere** (Effort-A leftover). The old structured "source map" (authoritative concept list injected into every content phase for source fidelity) was dropped in worklog [0028] — `pipeline.py:177` hardcodes `source_map_digest = ""` and `source_map_ids = set()`, and the injection in `_build_master_prompt` (`agent.py:512` `if source_map_digest:`) is always false. Yet `source_map_digest` is still a param on ~10 functions across `agent.py` (`:483/575/1584`) and `pipeline.py` (`:281/369/541/...`), and `source_map_ids` rides along too. Grounding moved into each phase's own `## Source Extraction` block (saved in `phase_outputs.output_md`). Rip out the dead params/threading. **Same bucket as the api-3 Group-2 residual:** api-3 (worklog [0061]) removed the classify/difficulty *cluster* (event/schema/column) but deliberately LEFT the `difficulty=None` parameter threaded through ~20 `agent.py`/`pipeline.py` sites — it overlaps R13's `pipeline.py`, so it was deferred to this same deadcode pass. Do both (`source_map_*` + `difficulty=None` threading) together once R13 has landed.

**Robustness / data / features**
- ~~Scanned / image-only PDFs: TOC extraction unsupported~~ **SHIPPED 2026-06-17 (worklog 0072, `feat/scanned-toc-vision`).** Both halves of the scanned-book story are now done: lesson-extract (0070) + TOC-extract (0072). `agent.extract_toc` now detects a sparse/watermark text layer (`extract_text_is_too_sparse` over the front+tail excerpt), drops the junk text, and vision-attaches a bounded **front+back page-window** (`_toc_source_pdf`, configurable `extract_toc_front_pages`/`extract_toc_back_pages`) so the provider OCRs the contents page (front OR back). **Proven on the canonical book `a0173601`** (was 0 entries → now **39 real entries** with titles+pages, read from the back-of-book TOC via claude-cli vision). Residual (NOT this item): the YAGNI boundary — a book whose contents page falls outside a 12+20 window now fails *actionably* ("widen extract_toc_*_pages and re-extract"); and the `/Gxx` glyph-loss TOC garble is still `extract-1`/R10.
- ~~`api-vision-1`: scanned-book vision (TOC + lesson-extract) forced `transport=cli` → fails OAuth on an all-Vertex head~~ **✅ SHIPPED 2026-06-26 (worklog [0094], `api-vision-1`).** `api_transport._gemini` now accepts PDF/image attachments (multimodal `Part`s over Vertex; claude stays text-only), and the `extract_toc` vision branch + `summarize_lesson_vision`/pipeline keep `transport="api"` for gemini so scanned books OCR over the Vertex SDK instead of the OAuth-failing CLI — an all-Vertex head needs **no gemini CLI login**. `auth_mode`/`error_message` follow the real transport. Proven by a real Vertex multimodal smoke (`scripts/smoke_api_vision.py`, 1315 prompt tokens = PDF ingested). Residual (out of scope): a >20 MB vision window would exceed Vertex's inline limit → would need the Files API (e020d51f's 32-page window is only 5.43 MB, fine); claude-api vision still cli.
- `opencode` is **too flaky to be a *primary* provider** — live-run 2026-06-04 (job `6a760767`, §0031): hung the full **600s** per-attempt timeout on **every** failover wave, blowing the 1800s job budget → failover+resume rescued it but burned 2 attempts (~35 min wall). Keep as last-resort fallback ONLY; never *request* it. Consider a shorter per-attempt timeout for known-flaky providers (the failover chain + resume already handle it gracefully). (Was: "never run against a real install" — now run, and it hangs.) **Narrowed 2026-06-25 (audit): the "keep last-resort" half is already true (`failover_provider_order` ends in `opencode`, `config.py:99`); only the shorter-per-attempt-timeout-for-flaky-providers part remains open (`per_attempt_timeout_seconds` is a uniform 600s).**
- Bad book data: math-algebra book `9e7833bc…` has a 4 KB stub PDF (not a real textbook) — clean up or replace.
- Confirm the English grade→CEFR ladder against the official Uzbek curriculum — needs curriculum-owner sign-off (external).
- _(2026-06-05, DEFERRED — not now, not soon)_ **Notion archive validator** — fully spec'd (`78b0c73`, `docs/superpowers/specs/2026-06-04-notion-archive-validator-design.md`) + TDD-planned (`9aad1ae`, `…/plans/2026-06-04-notion-archive-validator-plan.md`) but **NOT executed**. Auto, best-effort structural check that the live Notion tree matches `_HOMEWORK_LAYOUT`/`PHASE_TITLES` after `archive_job`, recording `homework_jobs.notion_validation` (verified/mismatch/archive-incomplete/skipped). Parked by decision; revisit only if archive correctness becomes a real pain.
- _(2026-06-04, blocked on the deferred validator above)_ If/when the notion-archive validator is built, surface its `homework_jobs.notion_validation` result (verified / mismatch / archive-incomplete) in the operator console. **Note:** neither the validator nor the `notion_validation` column exists yet — this is downstream of the parked work above.
- **✅ SHIPPED [0087]** (cluster-7 — infra warnings no longer co-mingle: `warnings = outcome.warnings if outcome.available else []`; `judge_status` serialized on `PhaseOut` + a distinct preview-console chip) _(2026-06-04)_ Phase judge writes `"judge-unavailable: <ExcType>"` into `phase_outputs.validation_warnings` on CLI/parse failure — an *infra* signal that renders in the console like a *content* defect. Distinguish them (separate field, or a prefix the console styles differently) so operators don't chase phantom content issues. (Code-review nit from the LLM-phase-validator build.) **Narrowed 2026-06-25 (audit): a distinct `judge_status` column WAS added (cluster-3, `phase_output.py:35`), but the `"judge-unavailable: <ExcType>"` string STILL co-mingles into `validation_warnings` (`pipeline.py:1025`) and the FE doesn't surface `judge_status` → only the observability/FE-filtering half remains open.**
- _(2026-06-02)_ Notion anchor **auto-resolve** (ties to Phase 2): resolve the subject-page ID by crawling (grade → `{N}-sinf` → child matching the subject label) instead of the hand-maintained `NOTION_SUBJECT_PAGES`. Would eliminate the **silent per-subject skip** (Kimyo incident). Surface unmapped skips in the UI/job result either way.
- ~~`notion-archive-1` (→ ROADMAP **R15**): `archive_job` **swallows push failures** — a failed Notion push leaves `notion_archived_at` AND `notion_skip_reason` both NULL (invisible, no auto-retry).~~ ✅ **SHIPPED 2026-06-26 (worklog [0086])** — `_push_with_retry` (3× backoff) + `_record_skip` persists `notion_skip_reason="push error: <Type>"`; re-archive affordance = `POST /jobs/{id}/retry-archive` + FE button.
- `notion-archive-2` (→ ROADMAP **R16**): subject-page resolution is **filename-substring-fragile** — for history (split Jahon / O'zbekiston tarixi) the page is chosen by matching the keyword against the book filename; `"ozbekiston"` won't match a **"Tarix Ozb"** book → silent "no Notion page" skip. Latent (no O'zb-history book uploaded yet); the `"jahon"` side verified resolving correctly 2026-06-17.

- `l2-bridge-follows-medium` — for L2 language-class subjects (English/Russian classes), make the scaffolding/bridge language follow the chosen medium (e.g. a Russian-medium school's English class bridges through Russian) instead of always Uzbek. Deferred from the 2026-06-29 multi-language feature (decision: medium affects only non-L2 subjects). Requires parameterizing the hardcoded-Uzbek L2 prompt rules.

### Frontend

- ~~`fe-subject-variant-1`~~ (→ ROADMAP **R17**) — **✅ CLOSED (2026-06-25 code audit): IMPLEMENTED — cards render `subjectLabelWithVariant`/`subject_variant` (`launcher.tsx:473`, `batch-funnel.tsx:109`).** Original: batch / library / launcher cards show only **"History · grade N"** — when both Jahon Tarixi and O'zbekiston Tarixi books exist for a grade they're **indistinguishable** in the UI. Surface the book/variant (filename or a Jahon/O'zbekiston label). Screenshot 2026-06-17.

### Database / Persistence

> Catalogue of data-model / persistence / job-lifecycle issues. Scope = schema shape,
> column semantics, row lifecycle, data integrity, and how persisted state is (or isn't)
> used on retry/resume. NOTE: items here may have their *fix* in pipeline/worker code
> even when the *symptom* is about persisted state — tag each with where the fix lives.

- `claim-gate-null-judge-hardening` — defensive loud-fail for NULL judge_provider/judge_model at the claim gate (latent: not reachable today given Task 2 stamping + 0037 backfill; a future unstamped-creation path would silently strand (#1) or mis-claim (#2)). Adversarial review 2026-06-27 traced both; clean loud-fail = non-NULL assert in jobs_repo.create, deferred because it breaks 10+ existing test callers (non-trivial). Revisit if a new job-creation path is added.
- _(none open)_ — phase-level resume shipped in [0031]; see **Done / promoted**.

## Done / promoted

- **`cancel-race-1` — ✅ FIXED 2026-06-18 (cluster-2, worklog [0078]).** Guarded `set_status` (terminal frozen; cancelling→only cancelled) + done-phase freeze; cancel signal now sticks.
- **`fleet-ctrl-1` — ✅ SHIPPED 2026-06-18 (cluster-2, [0078]).** `POST /jobs/batch/{id}/cancel` (cancel_all_in_batch).
- **`fleet-ctrl-2` — ✅ SHIPPED 2026-06-18 (cluster-2, [0078]).** `POST /jobs/batch/{id}/resume` (resume_failed_in_batch; reuses done phases).
- **`fleet-relaunch-dataloss-1` — ✅ FIXED 2026-06-18 (cluster-2, [0078]). Deliverable CHANGED to steer-to-resume** (supersedes the brief's "confirm-then-discard"): relaunch resumes failed/cancelled sections (reuse saved phases) instead of discarding; strict zero-write preview + relaunch_mode resume|discard (discard is the confirmed escape hatch).
- **`fleet-test-4` — ✅ DONE 2026-06-18 (cluster-2, [0078]).** Real per-job retry endpoint exercised via batch resume; live run is the 0078 acceptance gate.

- **`fleet-failover-1` — ✅ FIXED 2026-06-17 (fleet-robustness-fixes).** Failover no longer dies with a confusing `<prov> CLI not found` on a single-CLI worker: new `agent.provider_cli_installed(name)` (shutil.which over the provider's binary_names), and `_run_with_failover` skips any FALLBACK provider whose CLI isn't installed (the REQUESTED provider is never skipped — a missing requested CLI is a real surfaced error; api transport never reaches fallbacks). Tests in `test_failover_driver.py` (skip-uninstalled + requested-never-skipped + an autouse `provider_cli_installed=True` so the logic tests stay box-independent).
- **`toc-empty-ready` — ✅ FIXED 2026-06-17 (fleet-robustness-fixes).** TOC extraction that yields 0 entries no longer silently marks the book `toc_ready` with an empty lesson list — `toc_extractor.run` now raises on `not extracted.entries`, taking the existing `except` path (`status="failed"` + `error_message` + `error` SSE event the FE already renders). Surfaced live by scanned-PDF book `a0173601` (algebra g9, 0 lessons). Test: `tests/services/test_toc_extractor.py` (0-entry → failed/no-persist; non-zero → ready).
- **`r13-integrity-1` — ✅ FIXED 2026-06-17 (fleet-robustness-fixes).** `ensure_book_pdf_sync` gained an optional `expected_size` (the book's `file_size_bytes`, threaded from `pipeline.py`): a wrong-size cached PDF is dropped + re-fetched (ONLY when `fleet_head_url` is set — never deletes the head's canonical copy), and a truncated download is rejected before promotion. Stops a corrupt/partial cache from failing every job at extract (`pypdf: Cannot find Root object`). Surfaced live on worker `Oliver` (book `50b71915`). Tests added to `tests/services/test_book_fetch.py` (refetch-on-mismatch, reject-truncated, head-trusts-existing, correct-size-fast-path). _(A stricter sha256-vs-`content_sha256` check is left as a future tightening; size catches the observed truncation mode.)_

- **`api-3` (classify/difficulty cluster) — ✅ SHIPPED 2026-06-16 (PR #8, worklog [0061]).** Removed the dead `difficulty_classified` event, `DifficultyClassifiedEvent`, `set_difficulty`, `JobOut.difficulty`, `schemas/classify.py`, and the `homework_jobs.difficulty` column (migration `0026_drop_difficulty`, applied to the head DB). FE consumers removed + the "classifies the lesson" upload/section/job copy reworded (closed the separate 2026-06-02 copy item). **Group-2 residual** (the `difficulty=None` param threaded through ~20 `agent.py`/`pipeline.py` sites) deliberately deferred to the `deadcode-1` pass — it overlaps R13's `pipeline.py`.
- **Staleness audit 2026-06-11 (two adversarial sub-agent sweeps + controller re-verification, post-Phase-4):** removed **SSE teardown noise** as FIXED (`/toc/stream` now has proper `is_disconnected()` disconnect handling, `books.py:215` — the 0039 SSE-leak fix era); rewrote `fetch-1` (the 20MB fetch reject is gone — fetch now accepts 50MB like uploads); refreshed `api-3` line refs. ROADMAP same pass: **R12 CLOSED** (extract failover + local pypdf read live; residual → `extract-2` above), **R9 NARROWED** to tables+code-fences (SVG half moot — placeholder architecture). R10/R11/R13 re-verified still open.

- **Phase-level resume + faster orphan-reclaim — ✅ SHIPPED (worklog [0031], 2026-06-04).** Both former Open items closed by the job-resilience effort: (1) "No phase-level resume on job retry" — the pipeline now reads prior `done` rows and skips them (`pipeline.py` `_done_phase_md`/`_pending_phases`, always-on; `force`=new job, `/retry`=resume). (2) "Orphan-reclaim window hardcoded `job_timeout × 2`" — replaced by `reclaim_stale_seconds` lease-TTL (heartbeat-gated) + startup orphan-reset of `running` jobs. Also delivered: per-phase provider failover (`_run_with_failover`, classifier, claude reserved-for-user) + `phase_outputs.provider` attribution. Live-verified (job `6a760767`). Follow-on observability gap tracked as **R11** in ROADMAP.
- **Backlog audit 2026-06-03 — removed as STALE** (Effort A md-per-phase flip made them moot, verified by code check): memory-check `why_prompt`-not-schema-enforced + `MemoryCheckKind` 3-vs-6 question types (schema `app/schemas/memory_check.py` **deleted**); `api-2` `source_map_ready` / `concept_fidelity_warning` event schemas (**source map removed**); source-fidelity detect-only invented-`concept_id` warning (`_unknown_concept_ids` **removed**); `mistake_provenance` (`source`|`inferred`) tag on the CBP common-mistake (was a deleted schema field, **not** preserved in prompt form). None of these reference live code anymore.
- **Boss Arena 3-tier hint ladder — ✅ already implemented as prompt content** (`prompts/_general/boss-arena.md:25-28`: Hint 1 → Why, Hint 2 → How, Hint 3 → synthesis, never the answer). The schema (`boss_arena.py`) is gone but the ladder lives in the prompt.
- **Frontend trio — ✅ FIXED 2026-06-02 (Nggaev-v2, commit `e582f53`; tsc + vite build clean. Pending: user browser-verify).** (1) `frontend-6` — `job.tsx` `DonePanel` now counts the v2 columns (source concepts · case checkpoints · memory checks · Boss Arena questions w/ legacy final-challenge fallback · practice-arc games), so v2 jobs show real done-stats. (2) **Grade input** — optional Grade select (1–11) added to `upload.tsx`, wired `api.uploadBook → POST /books` `grade` field (no more SQL for new uploads). (3) `<Select>` uncontrolled→controlled — model picker `section.tsx` now `value={model ?? ""}`. _(Note: DonePanel was later rewritten in Effort A to count phases/warnings instead of `*_json` columns.)_
- W1 — `opencode` as 5th CLI provider — ✅ DONE 2026-05-29 (commit 8a96435), see [[MASTER_MEMORY]] §0010. (Verification follow-up moved to Open above.)
- ~~Stale `main` test files break `pytest tests/`~~ — **REMOVED 2026-06-01 (stale/not-applicable).** Those files (`test_flows.py`, `test_providers_registry.py`, `test_agent_models.py`, `test_api.py`) don't exist in our tree — they were the audit machine's untracked locals.
