# Project Roadmap — worked-up items

> Items promoted from [WISHLIST.md](./WISHLIST.md) once understood. Each entry states:
> **Issue** (what's wrong / wanted) · **Root cause** (why, with code/doc references) ·
> **Deliverable** (the concrete result after the fix). Move to "Shipped" when done.
> Tracked in git (committed alongside the work).
>
> **Everything above the "Shipped / Closed" ledger is OPEN/in-progress.** When an item
> ships, delete its detail block and drop a one-line record in the ledger — the full
> detail lives permanently in the worklogs (`MASTER_MEMORY.md`) + git. Do not leave
> shipped detail sitting in the open list (it reads as unfinished work).

---

## R9 — Notion push: markdown tables + code fences render as plain paragraphs (SVG half MOOT)

> **NARROWED 2026-06-11 (staleness audit):** the inline-`<svg>` half is **architecturally moot** — content phases now emit described placeholders (`![visual: … ](placeholder)`, `agent._PLACEHOLDER_RULES`), and `markdown_to_notion_blocks` renders those as callouts (`notion/blocks.py:127-137`); no new content carries inline SVG. What remains: **markdown tables and ``` code fences have no branch** in `blocks.py` (verified — zero table/fence handling) and fall through as escaped paragraph text. Deliverable shrinks to (b)+(c) of the original below; (a) SVG-rasterize is dead unless legacy pre-placeholder packets are re-pushed.

- **Issue:** the Phase-1 Notion push writes readable blocks, but an inline `<svg>` diagram (and markdown tables, e.g. teacher-note grids) shows up on the Homework page as raw escaped `<svg…>` / pipe-table text instead of an image/table. Ugly but non-blocking — **observed live 2026-06-02** on Homework page `37399838-1c76-81e8-a547-db74b8c1dbbc` (job `442cc7c4`, Geometriya 1.1). The per-phase `.md` attachment at the top of each leaf page (post Effort A [0028]/regroup [0029]; `homework.md`+`content.json` are gone) still carries the real diagram, so no data is lost.
- **Root cause:** `markdown_to_notion_blocks` (`app/services/notion/blocks.py`) only handles `#/##/###` headings, `---` dividers, `-/*` bullets, and inline bold/italic. SVG markup, markdown tables, and code fences have no branch → they fall through into paragraph rich-text and get escaped. Accepted as a known Phase-1 limit in the plan/spec.
- **Deliverable:** extend the converter — (a) detect an `<svg>…</svg>` block → rasterize or upload as an image/file block via the existing `upload_bytes` (image block), (b) markdown tables → Notion `table` blocks, (c) ```` ``` ```` fences → `code` blocks. **Tie-in:** fold into the upcoming **prompt-reshaping** pass — the SVG-emission policy (`_SVG_PHASES`) governs which phases emit SVG at all, so reshaping may reduce stray SVG in non-visual phases and should be coordinated with the converter fix.

---

## R11 — Provider failover is not legibly recorded (which model ran a phase, and that it switched)

- **Issue:** after per-phase provider failover (shipped on `Nggaev-v2` 2026-06-04, job-resilience branch), you can tell which provider *finished* a phase but not cleanly that it **switched** or which model actually ran. **Observed live 2026-06-04** (resume+failover smoke, job `6a760767`, history): `case-based-preview`/`flashcards` failed over `opencode → codex → gemini`; the DB shows the end state but not the journey.
- **Root cause (verified in the live data):** three gaps.
  - (a) **`phase_outputs.model_name` goes stale on failover.** `_execute_phase` sets `model_name = phase_model_label` (the *requested* model) at `create_or_reset` time; the new `set_status(provider=produced_by)` corrects `provider` but **not** `model_name`. Live: the gemini-produced rows read `provider=gemini` yet `model_name=opencode/mimo-v2.5-free` — self-contradictory. Refs: `app/services/pipeline.py` `_execute_phase` (create_or_reset model_name + final `set_status`).
  - (b) **A timed-out/cancelled attempt leaves NO `agent_usages` row.** `_run_with_failover` wraps each attempt in `asyncio.wait_for(per_attempt_timeout)`; on timeout it **cancels** `run_phase_prompt` before that call records its usage row. Live: `agent_usages` for the phase shows `codex(f)×2 → gemini(t)` but **no opencode row at all** (the 600s hang is only in the ephemeral server log). Refs: `_run_with_failover` TimeoutError branch (`pipeline.py`), usage recording inside `agent.run_phase_prompt`.
  - (c) **No explicit "failed over" signal.** A switch is only *inferred* by comparing `phase_outputs.provider` ≠ the job's requested provider, or by counting `agent_usages` rows per `phase_output_id`. There is no failover flag/count or ordered provider trail.
- **Deliverable (LOW PRIORITY — failover is rare under the current single-host/claude-default usage; matters for the 24/7 autonomous effort's *observability* pillar, see `docs/PRODUCTION_AUTONOMOUS_GENERATION.md` §5):** (1) on failover success, update `phase_outputs.model_name` to the producing model so provider+model agree (needs `run_phase_prompt` to surface the resolved model, since fallbacks pass `model=None` → provider default); (2) record an `agent_usages` (or dedicated failover-event) row for timed-out/cancelled/failed attempts so the full chain is captured; (3) optionally a structured per-phase trail (e.g. `["opencode:timeout","codex:hard","gemini:ok"]`) or a `failover_count` on `phase_outputs`.

---

## R16 — Notion subject-page resolution is filename-fragile (Jahon vs O'zbekiston Tarixi)

- **Issue:** for an app-subject split across multiple Notion pages (history → **Jahon tarixi** / **O'zbekiston tarixi**), the correct page is chosen by **substring-matching the keyword against the book filename**. This works for `"jahon"` (book `7-sinf_jahon_tarixi_2022.pdf` resolved to the jahon page, **verified 2026-06-17**), but an O'zbekiston-Tarixi book named **"Tarix Ozb"** would **not** contain `"ozbekiston"` → resolver returns `None` → silent "no Notion page" skip. **Latent** — no O'zb-history book is uploaded yet, so it will bite on the first one.
- **Root cause:** `_resolve_subject_page_id` (`app/services/notion_archive.py:43`) folds the filename `hint` and tests `_fold(keyword) in folded` for each keyword in the dict-form `NOTION_SUBJECT_PAGES` value; the keyword is the literal `"ozbekiston"`/`"jahon"`, so any abbreviated/variant filename misses. Same fragility family as the WISHLIST "Notion anchor auto-resolve" item.
- **Deliverable:** make variant matching robust — (a) require uploaded O'zb-history filenames to contain `"ozbekiston"`, **or** (b) add aliases (`"ozb"`, `"o'zbekiston"`, …) in the mapping / a normalizer, **or** (c) fold into the broader anchor auto-resolve crawl. Guard against loose-match collisions (a too-broad `"ozb"` could mis-match unrelated books).

---

## R18 — Fleet tray grows monotonically; needs a "Complete" split (blocked on a books-list progress rollup)

- **Issue:** since PR #24 / worklog [[MASTER_MEMORY]] §0071 removed the `!fullyBatched` exclusion (which was wrongly making launched books vanish), the Fleet "to launch" tray now keeps **every** prepared book forever — launched and fully-complete books never leave it. Fine for a handful of books; for the Oct/Mar all-subject campaign ([[oct-mar-content-campaign]], potentially hundreds of books) the tray becomes an unwieldy wall.
- **Root cause / why deferred:** an honest "Complete" section (or collapse-done) can't be built at the **group/tray** level today — `GET /api/v1/books` returns `toc: null`, so the tray only knows a book's *batches*, not its per-lesson done counts. Per-lesson truth currently lives only inside each card's lazy `getBook` fetch (`launcher.tsx` `ReadyCard`). A batch-level "complete" split would re-introduce the over-claim bug 0071 just killed (batch-complete ≠ all lessons done — a partial launch reads as complete).
- **Deliverable:** add a **books-list per-lesson progress rollup** (backend: include `lessons_total` + `lessons_done` per book on `GET /books`, or a dedicated `/books/progress` endpoint), then split/collapse the Fleet tray so completed books move to a "Complete" section (or fold away), keeping "to launch" scoped to ready + in-progress. A client-side per-book `getBook` fan-out is a possible FE-only stopgap but is N-queries; the backend rollup is the clean shape. Pre-campaign nice-to-have, not blocking current scale.
- **Also blocked here — `monitor-card-clarity-1` item 2(b) (the accurate combined total):** the monitor batch card now shows an honest per-transport note (`done 18 CLI · 13 API`, worklog 0093, item 2a) but deliberately does NOT show a combined book total, because a naive sum over-counts when the same lesson is done on both CLI and API (real overlap, proven on book `62865c70`: cli_done 1 + api_done 2 = 3 > 2 distinct). The accurate version — `X/31` = **distinct** lessons done on *any* transport — needs the same backend book-level per-lesson **dedup** rollup as the Fleet-tray split above. Ship both off one endpoint.

---

## R19 — Notion textbooks are stale / missing for several grade×subjects (campaign C3 blocker)

- **Issue:** a 2026-06-17 currency audit (6 grade-band sub-agent sweeps + controller verification) found several Notion textbook PDFs are **old editions or absent**, so homework generated from them would teach outdated content.
  - **Confirmed stale:** G8 Adabiyot (2019 → 2025 exists), G8 Ona tili (2019 → 2025), G5 Biologiya (2020 → 2024), G8 Informatika (2022 → 2023; the stored file may also be a mis-uploaded Russian-language edition).
  - **Likely stale** (year-less filename, but a newer edition is known to exist): G9 Adabiyot + Ona tili (2025), G5 Tarix (2025), G11 O'zbekiston tarixi / Jahon tarixi / Huquq (2025 reform).
  - **Missing in Notion entirely:** G6 Adabiyot, G10 Iqtisodiyot asoslari, G3 Jismoniy tarbiya.
- **UPDATE 2026-07-06 (G11 math: RESOLVED as current-but-hostile, not stale):** a full math/geometry sweep of `@elektron_darslikbot` (Telethon, G5–11; session `~/.tg-textbook-sweep/`, inventory.jsonl) shows our math shelf **matches the newest available edition at every grade** (G5/G6 2024 «AHA», G7/G10 2022, G8/G9 2019, G11 2018) — the bot tracks new editions (carries 2024s + «Huquq (2025)») yet offers nothing newer than 2018 for G11, so the earlier pdfbox "yangi darslik" lead was a relabel of the same 2018 book; the reform hasn't reached G11 math. File-quality check (haiku diff, controller-verified): our P1 is the COMPLETE 192-page master while the Telegram P1 is truncated at 112pp (algebra half only — likely the source of "incomplete" impressions); our P2 is byte-identical to the bot's. **Keep our copies; nothing to replace.** The 2018 book remains structurally hostile (two courses per volume, TWO mundarija pages → extractor+validator both defeated; TOCs hand-curated, worklog 0115) — recheck for a yangi-avlod G11 when the reform ladder reaches it.
- **Root cause / context:** the new-generation ("yangi avlod") reform re-authored **humanities/languages** (2022–2025) but **not** (as of the 2026-06-17 audit) the math/science core (Algebra/Geometriya/Fizika/Kimyo/Biologiya/Geografiya are still 2019 = current, not stale) — see the 2026-07-06 update above for the math-edge correction. Audit-method caveats worth re-using: Notion textbook PDFs are **private attachment URLs** → can't be WebFetched to auto-date a year-less filename (needs the authenticated Notion client / a cover read); grades 5–9 filenames usually carry a year, grades 1–4 and 10–11 usually don't. Authoritative latest-edition sources: `uzbmb.uz/info/textbooks` (Ministry list, published as **images** — not scrapeable; also cert-blocks WebFetch), `qogozcha.uz/darsliklar` (Ministry-approved, but per-book **years are JS-rendered**); usable fallbacks: `idum.uz` (edition history), `ziyouz.com`, `googol.uz`, `infoedu.uz`, plus the PF-134 (2022) curriculum-reform decree.
- **Deliverable:** for each confirmed/likely-stale entry, replace the Notion textbook with its **current edition**, and add the missing ones — an **operator task** (source the new-edition PDF → re-upload or re-fetch from Notion → re-extract TOC); for the "likely stale" set, first re-verify the year against an authoritative source above. This gates the Oct/Mar campaign's **C3 textbook-readiness sweep** ([[oct-mar-content-campaign]]) — a stale source PDF means stale content for **every** lesson generated from that book.

---

## Open follow-ups spun out of R21 (2026-07-02) — R21 core ✅ CLOSED, see ledger

- **R21.7 — solver-recall gap (accepted, gemini-only).** The CQ-C solver misses **conceptual truth-value** key errors outright (e.g. `y=k/x` Oy-symmetry, `263d99c5`; 0 catches in any run) and catches **expression-equivalence** (`8f734563/rlc`) and even the objective sign class only **probabilistically** (~1/3 and ~2/3 of gate runs respectively) — a characterized **model-capability** miss on the current gemini frontier (`agrees=True, 0 discrepancies`; a truth-value-directive prompt variant does NOT recover it, ×3; evidence `scripts/cqc_solver_characterize.py`). **Accepted, not fixed:** gemini-only is standing policy, so no claude-opus swap; coverage for these classes is **CQ-E's answer-key audit rubric**. Revisit only if a stronger gemini tier ships. Free cross-check: CQ-E's `answer_key` rubric scorer runs against `263d99c5` in its `--audit-check` — if that differently-framed scorer also endorses the wrong key on gemini, that's independent confirmation the gap is capability, not framing.
- ~~**R21.8 — solver-config editing.**~~ **SHIPPED (worklog 0118, `feat/solver-settings-badge`).** The solver global default is now operator-editable at `/settings` (`launch_defaults._MUTABLE` + `settings.py` Out/Update/`_serialize` + the required-concrete+manifest and transport validation loops all extended with `solver`, line-for-line judge mirror; **required-concrete** — null provider/model → 422, since a null default would strand jobs in the claim gate) and `solver_status` renders as a phase-console chip (`preview.tsx`; **`mismatch_regen` = green "answer-key fixed"**, a success, not an error). Plus a footgun warning near both content pickers (`launcher.tsx` + `section.tsx`) when content = `gemini-3.1-pro-preview` (self-grade swap → claude → unclaimable on a keyless fleet). No migration (columns shipped in mig 0043). `PhaseOut` now serializes `solver_status` (was judge-only).
- **R21.10 — per-job solver override editor (deferred follow-up from R21.8).** R21.8 shipped the *global-default* solver editor; a per-launch override UI (launcher/section `RoleAgentControls` solver picker, like the judge picker) is not built. The stamping path already honors per-job `solver_*` fields if a future UI sets them — this is pure FE. Low priority (the global default + self-grade guard cover the fleet correctly).
- **R21.9 — boss-arena wrong-feedback residue.** The audit's `263d99c5` Q3 (feedback declares one side of a two-sides-both-right debate the winner) stays uncovered by CQ-A + CQ-C: boss-arena emits no diffable written key so the solver can't check it, and CQ-A's boundary rule doesn't address feedback correctness. A future judge/boundary enhancement (or CQ-E rubric coverage).

---

## Shipped / Closed

> One line per shipped/closed item — full detail in the cited worklog + git. Nothing here is open.

- **R21 — Content-quality cluster (boundary leakage + wrong answer keys) — ✅ CLOSED 2026-07-02 (CQ-A [0109] · CQ-B [0110] · CQ-D [0111] · CQ-C [0112]; re-audit 10/10 PASS 2026-07-03, GO for October).** All six deliverables shipped: boundary note, reflection fix, L2 bridge, content lint, answer-key solver, extract guards. Open residue lives in the follow-ups section above (R21.7/R21.9/R21.10).
- **R20 — Golden-set eval / quality-regression harness — ✅ CLOSED 2026-07-06 (harness CQ-E [0113]; baseline-freeze [0120]).** Offline scorer + frozen 5-lesson golden set + baselines on the final system; gated dims = language/reflection (deterministic), LLM dims advisory. Follow-ups: `R20-expand`, `R20-llm-rubric-tuning` (WISHLIST).
- **R15 — Notion archival silently swallows push failures — ✅ SHIPPED 2026-06-26 (worklog [0086]).** `_push_with_retry` 3× backoff + `notion_skip_reason` stamping + `POST /jobs/{id}/retry-archive` + FE button. Extended by batch re-archive [0106] and force replace-mode [0114].
- **R10 — Broken-font PDFs poison TOC extraction — ✅ CLOSED 2026-07-02 (CQ-D, worklog [0111]).** `_alpha_plausibility_ratio` floor 0.70 in Gate A + TOC gate routes garbled/mojibake to vision; `/Gxx` glyph case already recovered via `_decode_glyph_text` [0035]/[0036]; live gap was cp1251 mojibake; corpus 25/26 accept. Narrow glyph-repair case stays deferred under WISHLIST `extract-1`.

- **R17 — Frontend subject-variant label (Jahon vs O'zbekiston Tarixi) — ✅ CLOSED 2026-06-18 (worklog [0074], `feat/history-variant-label`).** Backend-derived non-persisted `subject_variant` on `BookOut` + batch rollup; FE `subjectLabelWithVariant` → "History · Jahon · grade N" in funnel + launcher + Library. Detector reuses `notion_archive._fold` (drift-guard test). Display-only — R16 (backend keyword robustness) + `history|5` config stay OPEN.
- **R13 — Fleet PDF distribution (workers read local disk) — ✅ CLOSED 2026-06-16 (worklog [0062]).** Pull-on-demand: `fleet_head_url` + `GET /books/{id}/source.pdf` + `book_fetch.ensure_book_pdf_sync` (fetch-when-missing, atomic write, size-integrity guard via `r13-integrity-1`), wired into the pipeline read-site. `var_dir` honored via `storage.book_pdf_path`. Proven cross-machine (2-PC live, `fleet-test-1`). Empty `fleet_head_url` = single-box behavior.
- **Fleet Phase 4 — CLI | API transport toggle — ✅ SHIPPED 2026-06-11 (`feat/autonomous-fleet-engine`), worklog [[MASTER_MEMORY]] [0053].** Per-job/per-batch `transport` enum (`cli` default · `api` claude+gemini only) for subscription-vs-pay-per-token benchmarking. 7 task commits (`49e2670`·`dfa3973`·`0468d62`·`7317280`·`aeb5fd8`·`21e799e`·`a200c20`): `transport`/`auth_mode` columns + migration 0024; batch key → `(book_id, transport)`; `_auth_env` per-call adapter (cli baseline for every spawn, api injects the active provider's key with a loud `RuntimeError` on missing); transport-scoped dedup; api=requested-provider-only failover + loud judge auth-fail; worker `has_api_keys` claim gate; `auth_mode` attribution + price map + per-transport `$`; FE toggle/badge/`$`. **Billed live smokes + gemini-price verification are operator-run** — `docs/runbooks/phase4-transport-operator-acceptance.md`. Deferred follow-ons: `fleet-api-1..5` in WISHLIST.

- **R2–R8 — ALL SHIPPED 2026-06-02 (Nggaev-v2), worklog [[MASTER_MEMORY]] §0022.** One ordered backend fix pass (brainstorm→spec→plan→subagent-driven, each commit controller-verified; suite 227→240 green): R2 size-gate extract subset (`40dab51`); R3 reflection conform (`576a529`); R4 game phase→mode Literal subclasses (`aedea9d`); R5 fail-fast validators incl. required `JigsawPayload.solution` (`3e5053e`); R6 TOC task-ref + >20MB guard hoist + stats-from-registry (`f40366b`); R7 timeout 600→1800 (`bebf730`); R8 trim `_SVG_PHASES` (`e3988a1`). Full per-item detail in worklog §0022 + git.

- **R12 — Extract phase single-point-of-failure — ✅ CLOSED 2026-06-11, worklog [[0035]].** Extract now runs through `_run_with_failover` (claude excluded) and reads the book TEXT locally via pypdf (`agent.py` `read_whole_book_text`) — no CLI file-read, so the `var/` gitignore block can't bite. Sole residual (blank `EXTRACT_PROVIDER=` → default config validator) demoted to WISHLIST `extract-2` (one-line footgun, mitigated by failover).

- **R14 — CBP prompt self-conflict → near-100% MAJOR regen tax — ✅ SHIPPED 2026-06-10 (`f5c4400`), worklog [[0052]].** The CBP prompt told the setup visual to be "self-sufficient" while self-check #6 forbade naming the tested concept → the visual caption leaked the checkpoint answer → judge MAJOR on the first attempt of essentially every lesson (5/5 models), paying +1 regen +1 Opus judge each time. Fixed with prompt edits (setup-visual scene-not-cause clause + DPE required line); proven first-pass PASS on gemini-3.1-pro + 2.5-pro, 0 warnings.

- **R1 — inert subject-prompt leftovers — CLOSED 2026-06-01 (won't-fix, accepted).** The dead `prompts/<subject>/*` files (stale `classify.md` refs + RLC reverse-test) stay on disk, unused. No action needed; they don't affect the live general-prompt path. Only relevant if `USE_SUBJECT_PROMPTS` is ever flipped on — revisit then.
