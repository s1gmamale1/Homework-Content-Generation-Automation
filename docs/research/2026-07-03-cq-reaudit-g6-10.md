# CQ re-audit — 10 packets, grades 6–10 UZ+RU — 2026-07-03: **GO for October bulk**

The verification run for Cluster 10 (ROADMAP R21 / Oliver's "Test generated homework"
card): the 5 originally-audited G8 lessons regenerated under the full October stack
(gemini-3-flash-preview content · 2.5-flash judge · 3.1-pro solver · `transport=api`,
tip `2cc3ebf`+) plus 5 fresh across-grade cells (G6, G7, G9, G10, and G8-**RU** from the
scanned book). Method identical to the 2026-07-01 audit (read real textbook pages,
taught-before-asked tracing, every answer key re-solved independently, student sim) —
11 auditor passes (one invalidated + retried, see "harness lesson").

## Verdict: 10/10 PASS-WITH-NOTES (was 5/5 FLAG)

| Defect class (original audit) | Before | After |
|---|---|---|
| Curriculum-boundary leakage (converse/alomatlar/asymptote) | 3/5 packets | **0/10** — one packet even self-fences ("teskari teorema … hali o'tilmagan") |
| Wrong answer keys (correct student graded wrong) | 3/5 packets | **1 item in ~420 keys re-solved** (G9 memory-check card_7: distractor D also true — dual-correct MC; solver passed it: implication-spotting is its miss profile) |
| Broken/unanswerable questions | 2/5 packets | **0 blocking** (one open-ended boss "What" with a false premise, no auto-key — y=k/x Q5) |
| Reflection fabricating outcomes | ~all | **0/10** (CBP §9 still hardcodes "Completion status: passed" — milder cousin, template-level) |
| Mixed-script splices / calques | 3/5, 2/5 | **0/10** / 0 ("qizil seld" gone; "keys-stadi"×1, "Qizil chiroq"×1 — new one-off calques) |
| Extract fidelity | 2/5 drift | vision extract on the scanned RU book reproduced the printed §15's own examples (`0·x>−5`, `0·x>1`) **verbatim** |

**All 14 original per-packet defects verified FIXED** by the 5 before/after pairs.

## Guardrails observed live
- **Solver first catch:** G10 memory-check `mismatch_regen` — solve→mismatch→regen→solve-clean;
  final 10/10 keys verified correct by hand. Cost of the event ≈ 1 extra gen + 1 solve.
- **Lint firing:** both `errdet_no_broken_marker` flags were vocabulary misses, not content
  violations (markers present as "(BU BLOK XATO)" postfix / "(Broken)" + `N-yorliq` naming;
  reveal header "Oshkor qilish"). Content gradable in both.
- **Judge:** `major_shipped` labels honest (off-lesson flashcard, setup-spoiler CBP).

## Measured cost (per homework, full October stack)
**avg $0.578** (min $0.43, max $0.78; solver total $0.88/10 jobs ≈ $0.09/hw).
October batch ≈ 3,500 packets ⇒ **≈ $2.0k**.

## Round-2 polish list (non-blocking, prompt/lint level)
1. **RU localization gap (top item):** Uzbek template strings in RU output — «Hali emas» ×8
   (rlc + boss wrong-feedback opener), «Кучли/Заиф томонлар» reflection headings, "Matematika/
   Algebra" title. RU is first-class per the 2026-07-02 meeting → fix the prompt templates.
2. `red herring` English token emitted by every RLC (the prompt uses the term) → rename in
   `practice-rlc.md` to an Uzbek/Russian equivalent.
3. CBP template: stop hardcoding "Completion status: passed" / past-tense completion pre-attempt.
4. content_lint vocab: `xato` postfix, bare `(Broken)`, `yorliq` as block noun, `oshkor`
   reveal header (both live FPs); consider `Hali emas`→RU check tie-in with the en-gate fix.
5. One-off content nits: G9 card_7 dual-correct distractor; y=k/x boss Q5 false premise;
   G8-geo boss Q2 placeholder numbers ≠ question numbers; typos (Szenariy×5, davmidagi,
   chiqanda, Shubhám×3, keys-stadi).
6. English scaffold headers (Scenario/Why/How/What/Feedback lines…) remain everywhere —
   decide once whether the FE consumes them as structure (then exempt) or they must localize.

## Audit-harness lesson (cost a phantom "catastrophic" verdict)
Parallel auditor agents shared one scratchpad and overwrote each other's generic dump
filenames (`packet.txt`, `phases.txt`) — three agents read a SIBLING's packet mid-audit;
one issued a false "cross-job contamination" FLAG (retracted; DB was always clean — verified
by direct query + `cmp`). **Rule: parallel agents write uniquely-named scratch files**
(e.g. keyed by job id). Added to the gatekeeper doctrine §6.

## Go/no-go
**GO** for the October bulk on quality grounds: zero boundary violations, zero blocking
questions, key-error rate ~0.24% of items (one subtle dual-correct MC in 420), all original
defect classes dead or reduced to polish. Round-2 list above ships as ordinary prompt/lint
follow-ups, not gates. Remaining bulk gates are operational: SCOPE matrix (user), R19
textbook sweep, cost-model decision (Vertex vs subs).
